# The draft's checks (September 17, 2026)

*One checker per section, each reading the draft against `RECORD.md` and the code. Not yet applied: this is the list to work through before the draft becomes `AUTHORITY.md`.*

## values (§0) — fixable

- **wrong-rule** — §0.2 The inputs are the outputs
  - "No rule may branch on a neuron's index, its zone or its label" is stronger than the record and is contradicted by rules the scope keeps. RECORD §2 on goo: "the input zone is the first `across` neurons and the output zone the last `outputs`, picked by index because there are no places to pick them by"; the wiring rule branches on zone membership ("no neuron projects onto itself and no input neuron onto another"); the drive forces input neurons and the read reads output neurons; and RECORD §5.2's escape draw is "one uniform per neuron per wave, in neuron order". The engine sentence "a neuron's place ... is an ordering and never a role" is wrong in the same way.
  - *fix:* State the record's value as the record has it: a neuron is an input, hidden or output neuron only with respect to where external connections are, and the neurons themselves operate identically. Then say what is actually forbidden — no neuron gets a different activation, firing or learning rule by its index, zone or label — and let the engine note say that a neuron's index selects which zone it is in and fixes the order draws are taken in, and carries nothing else.
- **wrong-attribution** — §0.2 attribution
  - "All neurons are first-class citizens of the population" is cited to [RECORD §2, §8]. It is at RECORD §4.3 (line 1558) and §8 (line 3597); §2 carries the permissive-structures sentence instead, and §3.4 has the record's own unquoted phrasing.
  - *fix:* Cite [RECORD §4.3, §8].
- **missing** — §0.3 A neuron integrates delta functions
  - The clause keeps only the first half of a non-negotiable. RECORD §0's second bullet is "A neuron integrates delta functions to determine its potential and fires when the integral exceeds a threshold, at which point it resets. This is an integrate-and-fire neuron." θ is kept for the rewrite (Byron, 14:53 MDT), so the half that was struck in the outline's reading is back in force, yet §0 nowhere says the neuron is an integrate-and-fire neuron that fires on a threshold and resets.
  - *fix:* State the bullet whole: a neuron integrates delta functions, fires when the integral reaches its threshold and resets — an integrate-and-fire neuron. The softness of that threshold (escape noise) is the firing clause's business; the value is that the level exists and the spike resets.
- **wrong-attribution** — §0.4 A neuron that fires is then absolutely refractory
  - "not a limitation to be worked around, and no rule may work around it" is an addition. RECORD §0 and §5.3 say only "This is a feedback control mechanism and computational feature of the system." The clause presents the extension as Byron and Cedric's from the beginning.
  - *fix:* Keep the record's sentence as theirs and either drop the extension or mark it: "Claude's reading of "computational feature", to be corrected in a word."
- **wrong-rule** — §0.5 Structures are permissive
  - "no quantity is clipped to keep it tidy" states a rule the system does not have. Weights are clipped: WEIGHT_RANGE [−1, 1], "random weights are drawn uniformly from this range, and learning clips to it" (RECORD §1.1), and the reinforce update is w_ij ← clip(w_ij + LR·A·e_j) (RECORD §6.7). What Byron struck was THRESHOLD_RANGE, the clamp on thresholds.
  - *fix:* Say what was struck: a threshold goes where homeostasis and un-sticking take it, and nothing clips it. Leave the weight range to its own clause rather than forbidding it here.
- **wrong-attribution** — §0.5 citation
  - "Please eliminate threshold clipping. It's artificial." is cited to [RECORD §1.3]. The quotation is at RECORD §3.4 (line 736); §1.3's struck THRESHOLD_RANGE row carries only the fragment "It's artificial".
  - *fix:* Cite [RECORD §3.4; §1.3's struck THRESHOLD_RANGE row].
- **wrong-attribution** — §0.12 The changes intended, and not made — first bullet
  - "Theta should also be deprecated" is quoted as Byron's and cited to [RECORD §0.3, §0.4; docs/rewrite-answers.md §1]. Those words appear in none of the three: RECORD §0.3 says only "with θ ignored", and rewrite-answers §1 paraphrases ("and "deprecate θ" is a later change"). The quotation is at docs/rewrite-outline.md lines 177 and 273, where it is attributed to Byron, September 17, 2026.
  - *fix:* Cite the outline (docs/rewrite-outline.md §6.2's note) as the provenance of the quoted words, and cite RECORD §0.3 separately for "θ ignored". Nothing else in the bullet needs changing — the 14:53 MDT counter-quote is right.
- **wrong-rule** — §1.1 What a neuron holds — engine note
  - "a checkpoint round-trips every one of them" is false for two items listed. RECORD §5.2 on the hazard's scaling by the count: "a checkpoint recomputes it rather than storing it, since it is a rule and not a state" — persistence.py line 375 recomputes `escape_scale` on restore. The epoch-local flags (fired in this epoch, forced this epoch, the spike count at the epoch's start) are not stored either; `reset` sets them at the epoch boundary a checkpoint is taken on.
  - *fix:* Split the note: the checkpoint carries the neuron's state — potential and its clock, threshold and floor as the run reached them, Δ_j, spike times and counts, r_j, p̂_j, n and E_j — and recomputes what is a rule rather than a state, the hazard's scaling by the count; the epoch-local flags are set by the epoch's reset.
- **wrong-rule** — §1.1 What a neuron holds — second bullet
  - "its threshold θ_j and its floor p^min_j — the starting values its container gave it, and wherever homeostasis or un-sticking have since moved them" attributes movement to the floor as well. Homeostasis and un-sticking move the threshold only (learning.py lines 508 and 542 move `neuron.threshold`, nothing moves `minimum_potential` after the container sets it in network.py line 218); RECORD §5.2: "Homeostasis and un-sticking (§1.3) move a threshold from wherever it starts".
  - *fix:* Say the floor is the starting value its container gave it and does not move, and that the threshold is that starting value and wherever homeostasis or un-sticking have since taken it — and note, as RECORD §5.2 does, that a checkpoint stores both as the run reached them.
- **wrong-rule** — §1.2 The potential is an accumulator of evidence
  - "once inhibition has taken the potential to p^min_j the potential is the floor whatever the weights, and the identity holds again from the next arrival" is imprecise about which identity, and wrong for the first one stated. After the floor bites p_j = p^min_j + Σ w_ij x_ij, not Σ w_ij x_ij; only ∂p_j/∂w_ij = x_ij resumes. And x_ij no longer counts "since its last spike": the floor clears every open arrival (neuron.settle → clear_arrivals; RECORD §6.7: the floor, a forced spike and a discharge "settle with no credit"), so it counts from the floor.
  - *fix:* State it as the record does — the two identities hold exactly while the floor has not bitten — then say that the floor clears the open arrivals, so the derivative identity resumes from the next arrival and x_ij counts from the floor rather than from the last spike.
- **missing** — §1.2 / §1 as a whole — the floor
  - The floor's rule is never stated, only referred to, and the reference points at the wrong clauses: "(the firing clauses set the floor)". In the record the floor belongs to integration — RECORD §5.1: "Inhibition (w < 0) pushes the potential down, and once per wave the floor applies: p ← max(p, MINIMUM_POTENTIAL)" — applied on the wave's summed input so the result does not depend on the order signals arrived in (RECORD §4.1; neuron.settle). Only the floor's *starting value* comes from the container's scaling in §5.2.
  - *fix:* Add a clause in §1: the floor applies once per wave, after the wave's signals have summed and before anyone fires, p ← max(p, p^min_j), which is what makes the wave order-independent; at the floor the open arrivals are cleared and settle with no credit. Point the container's scaling of p^min_j at the firing clauses separately.
- **wrong-rule** — §1.4 What a spike does to the potential
  - "the spike is the moment the learning rule settles what those arrivals earned" holds only under the accumulator. Under the leak each decision charges the synapses as it goes and the spike merely clears the traces (neuron.decide/fire; RECORD §6.7: "Under the leak the same rule is charged per decision with the leaked trace").
  - *fix:* Qualify: under the accumulator the spike is the moment the learning rule settles every open arrival; under the leak the charge was made at each decision and the spike only clears the traces. Also reconsider attributing t_prev, the memory of the spike before last, to "from the beginning" — RECORD §5.2 states it without a date and it is what the release law and the ISI factor read.
- **style** — §1.3 The leak, as a per-run option — engine note
  - "and the skip changes no bit, those factors being exactly 1" repeats the record's justification verbatim in substance (RECORD §5.1: "The skip changes no bit: those factors were exactly 1"). As written it reads as a finding reported in the specification.
  - *fix:* Phrase it as the requirement it is: under the accumulator no engine evaluates a decay anywhere, and the skip must change no result — the factors it skips are exactly 1.
- **wrong-attribution** — §1.7 The rate memory
  - Two small provenance problems. r_j starting at 0.5 is in the code (neuron.py line 78) and is nowhere in RECORD; and "From the pre-alpha, carried" is asserted where RECORD §1.3's RATE_MEMORY row carries no date and §6.7 only has the Teacher keeping r_j as part of the pre-alpha's rule.
  - *fix:* Say where the 0.5 comes from — "the start is the code's, `neuron.py`; not stated in the record" — or mark it as Claude's reading to be corrected in a word, and soften the dating to what §6.7 supports.
- **out-of-scope** — §1.8 The per-decision expectation — the Open note
  - "its window is not settled" is Claude's characterisation. What RECORD §1.3 leaves open is the value: "a starting value, to be swept". The window is measured there (about three epochs, from ~3,300 decisions an epoch), which is a measurement and rightly excluded — but the clause should not turn the measurement's existence into an open question.
  - *fix:* Open: DECISION_MEMORY is "a starting value, to be swept" [RECORD §1.3]. Nothing more.
- **style** — §0.6 The goal is the substance — the Open note
  - The note names dropped mechanisms — "the hex containers, lattice, spread" — inside Byron's quotation. It is framed as an open question and not as a clause, which the scope allows, but it is the one place in §0–§1 where the archived containers are named.
  - *fix:* Keep it if the naming is wanted to make the open question legible; otherwise trim to "the plane and its containers are archived (Byron, September 17, 2026, 15:08 MDT)" and let RECORD §2–§3 hold the list.

**Missing from this section:**
- CONFIRMED, "drop the codings": docs/rewrite-answers.md, "The codings, confirmed 15:10 MDT", settles it exactly as the scope has it — dropped are raw coding, population coding, population-complement, the Hamming and parity error-correcting codes and the input bit flips (FLIP); kept are complement coding on the inputs, complement coding on the outputs and the clock neurons, "what mnist runs". The earlier 15:02 entry's looser phrase "the codings and the error-correcting codes and the input flips, keeping complement coding and the clock neurons that mnist uses" means the same thing. No conflict; the draft's §6 scope is right on this point.
- , A clause stating the floor's application (once per wave, on the wave's summed input, before anyone fires; the open arrivals cleared there) — see the §1.2 finding. This is the one rule of the neuron in scope that §1 does not state anywhere.
- , A clause saying what survives the epoch boundary. Under the accumulator this matters more than it did under the leak: RECORD §4.2 step 1 — "Potentials are kept (`--discharge` zeroes them instead). Spike times are kept: the refractory period outlives the epoch." §1 says only that a spike resets the potential, so a reader cannot tell whether an epoch does. `neuron.reset(discharge)` also clears the open arrivals when it zeroes a potential.
- , One more piece of per-neuron state in §1.1's inventory: the credit the firing decision leaves for the spike to settle (`neuron.credit`), which under the accumulator sits beside E_j — RECORD §6.7, "a decision adds its q_j to E_j ... the spike settles every open arrival, e_ij += c_j x_ij − (x_ij E_j − B_ij)". E_j is listed; its counterpart is not.
- , §0 carries no value for the seed and the checkpoint, though §1.1's engine note leans on the checkpoint and the scope keeps "checkpoints, the input stream, resume". RECORD §7 states both as invariants: "A seed is the whole run" (shortcuts, weights, permutation, inputs and exploration noise all from the seed's stream) and "Checkpoints round-trip". If they are meant to live in a later section, §0.9 should point at them; if they are values, they belong beside it.
- , SCOPE CAUTION for the sections after this one (not a fault in the drafted text, which does not enumerate critics): the harness scope lists the critics as evidence, row and graded, with the fraction right kept as a reported measure and not a critic. Byron's last word is later and different — docs/rewrite-answers.md, "The class critic, kept (15:12 MDT): "keep the class critic". So four critics survive: evidence, row, graded and class — the 1-or-0 that pays when the label's class out-spikes every other, a tie losing. The fraction right reported beside the evidence reward is that critic's own number, so the reported measure and the critic are one rule again rather than two." Write the critic clauses from the 15:12 answer, not from the 15:02/15:09 list.

**Choices the drafter had to make:**
- Confirmed before drafting, as asked: "drop the codings" (rewrite-answers.md, 15:10 MDT) means drop raw coding, population coding, population-complement, the Hamming and parity error-correcting codes and the input flips (FLIP), and keep complement coding on the inputs, complement coding on the outputs, and the clock neurons — what mnist runs. No coding clause falls in §0 or §1, so nothing in my part turns on it; I have left the POPULATION ambiguity as an open item for the drive and the read.
- §0 is values only, and I introduced one kind-marker rather than a history section: a value marked "a direction" (§0.1) states an intention, binds nothing and overrides nothing. That lets §0.10, §0.11 and §0.12 name dopamine, exploration at the synapse, the deprecation of theta, the deterministic drive and small-world shortcuts as intentions without any of them reading as a clause.
- I did not write a "struck from §0" clause (the earlier outline's 0.11). The file's own rule forbids the history of what a rule used to be; the record holds the struck sentences. The dopamine bullet of RECORD §0 survives instead as the frame it always was — one global scalar (§0.10) — with its timing law simply absent.
- I did not state "the potential does not leak" as a §0 value, though the record's §0 said it as first written. The leak survives as a per-run option by Byron's own words, and a value saying otherwise would be contradicted by §1.3 on the day it was written. The accumulator is therefore stated positively in §1.2 and the leak as an option in §1.3, and §0.3 carries only the delta functions.
- I put "one authority, every platform" in §0 (§0.9) on Byron's word that it is "a foundational rule", and named the array engine's tolerance in the clause itself. The engines' own invariants section may carry the mechanism; if it does, §0.9 keeps the value and the tolerance moves.
- I kept REFRACTORY_HOPS in §1 (§1.6) because the record keeps it in "the neuron and its clock" and because Byron gave it in the same breath as the accumulator ("NOT leaky ... 2 hops"), pointing at the clock section for the hop's use by the schedule.
- I wrote p-hat_j's update in §1.8, since the task names the per-decision expectation as the neuron's state, and marked the warm start and the window as Claude's reading, as the record does.
- §1.1 lists the firing decision's width, the hazard's count scaling and the hazard's exposure moment among what a neuron holds, with a pointer, so that an engine builder has the whole inventory without my specifying rules that belong to the firing clauses.
- I cited the record by section in brackets and quoted Byron's own words at each decision, including the two the rewrite settled this afternoon (14:53, 14:56, 14:58, 15:02, 15:08 MDT), so a reader of both files meets the same words in the same terms.

**Left open for Byron:**
- What the substance is, now the plane and its containers are archived: §0.6 keeps "the goal is the substance, not a task" while the record's definition of walnut butter (a substance spread on the plane, one neuron per unit cell) goes with the hex containers. Byron's to say what butter means with no plane, or §0.6 stands with the record's definition cited and unused.
- DECISION_MEMORY's window is open: 10^-4 is "a starting value, to be swept" (RECORD §1.3), and the measurement that gives it a window in epochs stays in the record.
- Whether §0.11 (exploration at the synapse) belongs among the values at all: the record places it at the top at Byron's instruction and expressly not among §0's rules. I carry it as a direction, which contradicts no clause in force; one word from Byron settles whether it is a value, an appendix, or stays only in the record.
- Byron asked that the ISI factor's authority stand "near the very top". My §0 does not carry it, the factor being the learning rule's. Whoever assembles the file must decide whether §0.2 of the record becomes a clause inside §0 or the learning section defines it before anything uses it.
- The floor (MINIMUM_POTENTIAL and its fan-in scaling) is named in §1.1 and §1.2 as state my clauses depend on, but its value and its scaling belong to the firing clauses. If that section does not carry it, the floor has no clause and §1.2's identity has nothing to break it.
- §1.8 states how p-hat_j is kept because it is the neuron's own state; the charge that reads it is the learning rule's. If the learning section states the update too, one must cite the other rather than restate it.
- §1.6 fixes REFRACTORY_HOPS, which the record keeps under "the neuron and its clock" (§1.2) while the hop's use by the schedule is §4.1's. If the clock section fixes it as well, drop one.
- r_j's consumers — homeostasis, un-sticking, and what a report calls stuck — are named in §1.7 by pointer only; whether they are one clause or two in the learning section is not mine.
- "Drop the codings" drops POPULATION as an input coding; it must not be read as dropping mnist's output populations of three fire-if-one and three fire-if-zero a class, which are the kept output complement coding. The drive-and-read section needs to say so in its own words.
- §0.10 says no rule in force gives a neuron an error of its own, which is true once ADALINE is dropped and the teacher of §6.9 with it. Byron's to confirm in a word, since it forecloses a per-neuron error without his having struck one.

## synapse (§2) — fixable

- **wrong-rule** — §2.5 "Beside its weight a synapse carries exactly four pieces of state"
  - Not four. Under the leak — which scope item 3 keeps as a per-run option — the synapse also carries the moment its trace was last brought up to date (`Connection.trace_at`), which §2.6 itself then introduces ("which is why the synapse also keeps the moment its trace was last brought up to date") and which the checkpoint stores as the second half of each `traces` entry (persistence.py:105). The clause contradicts the next clause in the same section.
  - *fix:* Either name five (trace, the moment the trace was brought up to date, stamp, note, score) or say "four under the accumulator, five under the leak" — and keep the checkpoint sentence consistent with whichever it is.
- **wrong-rule** — §2.7 "This is the only trace of activity a target has of its own inputs"
  - Stale as of September 17, 2026, and self-contradictory here. The record's sentence is §4.4's, written when the stamp was the only per-synapse trace ("Each connection stamps the time of the last signal its target actually integrated: that is the only trace of activity the target has (§6.4)"). The single-spike rule of §6.7 gave the synapse a trace and a note, which §2.5 and §2.6 of this very draft describe.
  - *fix:* Drop "the only". Say what the stamp is for: the clock time of the last integrated signal, which the quash reads to find the synapses that carried a cycle. Cite [RECORD §4.4, §6.11].
- **wrong-rule** — §2.8 "the synapse adds $E_j$, the target's expected spikes since its last spike, to its note"
  - With the ISI factor on by default — scope item 5, RECORD §0.2, `ISI_FACTOR` on — a decision adds $f\,q_j$ to $E_j$, not $q_j$ (neuron.py:decide multiplies both credit and expectation by the factor before `self.expected += q`; §0.2: "a decision adds $f q_j$ to $E_j$, and the spike settles $f c_j x_{ij} - (x_{ij}E_j - B_{ij})$"). So $E_j$ is not the expected spikes; it is the charged expectation, ISI-weighted, and can be negative once $I$ exceeds 3 × REFRACTORY.
  - *fix:* Call $E_j$ the sum of the charged expectations of the target's decisions since its last spike, and say that the ISI factor of the firing/learning clauses weighs each one, or defer the naming of $E_j$ to the clause that defines the charge.
- **wrong-rule** — §2.5 and §2.8, the note $B_{ij}$ stated unconditionally
  - The note is the evidence accumulator's bookkeeping only. Under the leak (kept as a per-run option, scope item 3) nothing is written to it: propagation.py updates `payload.noted` only in the `accumulating` branch, and neuron.decide charges the leaked trace per decision instead, because — RECORD §6.7 — "the regrouping needs factors of $e^{t/\tau}$ that overflow within a second of run". As written, §2.5 gives every synapse a note in every configuration.
  - *fix:* Say in §2.5 and §2.8 that the note exists under the accumulator, where the spike settles every open arrival in one operation, and that under the leak the score is charged per decision on the leaked trace with no note [RECORD §6.7].
- **wrong-rule** — §2.9 "the score is cleared for the next read" vs §3.9 "each synapse's score with it"
  - Two clauses state where the score is cleared, and they disagree. RECORD §6.7 puts it at the read ("the read settles the debit so far, pays LR·A·e_ij, and clears the score"); the code clears it at the next epoch's reset (network.py:579, `connection.score = 0.0`, and re-bases the note there too, line 580) while the pay at the read only calls `settle_scores` (learning.py:614) and leaves the score standing. The two moments are equivalent only because nothing runs between them — which is a fact about the schedule, not a rule the reader can infer.
  - *fix:* State it once, in one clause, and say which moment is the rule: the read pays and the score is cleared before the next epoch's first charge. Have the other clause cite it instead of restating it.
- **wrong-rule** — §3.9 "while potentials, spike times, traces, notes, stamps and signals in flight are all kept"
  - The notes are not kept at reset. network.py:580 re-bases every note under the accumulator, `connection.noted = connection.trace * connection.target.expected`, which is the same "open arrivals counting from now" operation §2.9 describes at the read. Listing the note among the things reset leaves alone is wrong in the one configuration this specification makes the default.
  - *fix:* Either drop "notes" from the kept list and say the open arrivals' debit counts from the reset, or move the whole re-basing into §2.9 and have §3.9 say the reset clears the fired-this-epoch state and nothing else per synapse but the score.
- **wrong-rule** — §3.4 "a nanosecond up to a millisecond of clock time and growing with it"
  - Units. TOLERANCE = 1e-9 is relative and the clock is in nominal milliseconds (clock.py: `TOLERANCE * max(1.0, abs(time))`), so the slack below 1 ms of clock time is 10⁻⁹ ms — a picosecond, not a nanosecond. The clause is right that the record's "times are rounded to a nanosecond" is not what is built, then repeats the record's figure as if it were.
  - *fix:* Say slack$(t) = 10^{-9}\max(1,|t|)$ nominal milliseconds: 10⁻⁹ ms up to a millisecond of clock time, growing in proportion after. Since the brief says to spell out symbols and prefer the record's terms, name the discrepancy with the record's "nanosecond" plainly rather than inheriting it.
- **wrong-attribution** — §3.7, attribution: "Byron, September 14, 2026: 'rules in authority.md must be implemented cross-platform' [RECORD §6.15]"
  - The quotation is accurate and correctly dated, but it decides something else. In RECORD §6.15 Byron says it to refuse an engine that approximates the exploration draws; push order is the record's own note on a test ("signals due at one moment are summed in push order, so an engine that flattens the topology differently sums a wave differently and lands on different bits"), not Byron's decision. The clause hangs a summation rule on a quotation about cross-platform implementation.
  - *fix:* Attribute the summation order as §3.6 does — Claude's reading of what the three engines do, to be corrected in a word [RECORD §6.15] — and keep Byron's cross-platform quotation for the platform clause, where it is the rule.
- **style** — §3.7 "its exception ... is named in the platform clause and nowhere else"
  - The sentence names the exception ("every spike equal, continuous quantities to a part in $10^9$") in the act of saying it is named nowhere else. It also carries the record's finding about engines flattening topologies differently, which is an argument for the rule rather than the rule.
  - *fix:* State the rule alone — a wave's deliveries are summed in push order — and either give the array engine's tolerance here or in the platform clause, in one place, without the cross-reference that contradicts itself. Byron's own words for the tolerance are in docs/rewrite-answers.md ("keep, with tolerances, as a foundational rule; all platforms are built from one clear authority").
- **wrong-attribution** — §3.8 "[RECORD §5.2, §6.15: 'one uniform per neuron, in neuron order']", under "Byron, September 15 and 16, 2026"
  - The quoted phrase is the record's own prose in §5.2 ("The draw is one uniform per neuron per wave, in neuron order, from the exploration stream of §6.1"), not Byron's words, and it is not in §6.15 at all — §6.15 says only "The per-wave draw lands in propagation.py's position exactly: after the floor, before anything fires". Sitting under a Byron attribution, a bracketed quotation reads as his. Byron's actual §5.2 words are "Let's please go with (3). It is the most like what I want to do. Make the boredom stochastic and it is Williams's unit outright" (September 15) and the sqrt(N) instruction (September 16).
  - *fix:* Cite [RECORD §5.2] without the quotation marks, or quote Byron where he decided the escape noise and mark the draw's position and order as the record's statement of what the engines do.
- **wrong-rule** — §3.8 "Every neuron takes a draw whether it can use it or not, so the stream's position depends only on the count of neurons and the count of waves"
  - True only when the decision is a draw. `Network.explorer` installs the hook only when the network has a positive width (`self.hazard`) or additive noise, so at ESCAPE_DELTA = 0 — the deterministic limit RECORD §5.2 keeps word for word — no uniform is drawn at any wave and the stream does not move. As an unconditional clause it makes a run at Δ = 0 non-conforming.
  - *fix:* Open with the condition: where the firing decision is a draw (a positive width), one uniform per neuron per wave in neuron order, taken whether the neuron can use it or not; under the deterministic rule nothing is drawn.
- **out-of-scope** — §3.11 "**Open:** the mnist runs this specification states ran at 100 ms, set per run, so whether 35 ms is still the default is Byron's to say"
  - The record leaves nothing open here: RECORD §4.2 decides INTERVAL = 35 ms and that no problem overrides it, and no Problem sets one (problems.py's `interval` field is None everywhere; cli.py:841 falls back to INTERVAL). The openness is the draft's own invention, and the reason given for it — what the mnist runs ran at — is run history, which the brief puts in RECORD.md. The brief's rule is that a clause says what is open in Byron's words; there are no words here, and docs/rewrite-answers.md does not raise INTERVAL.
  - *fix:* Delete the Open paragraph. Keep the rule: INTERVAL is 35 ms, no problem overrides it, a run may set it for the whole of that run. If the 35-vs-100 question is worth putting to Byron, put it to him rather than into the specification.
- **style** — §3.11 "the record's words for his decision, not a quotation of it: 'INTERVAL is 35 ms, and no problem overrides it'"
  - The clause disclaims the quotation and then sets it in quotation marks anyway, which is exactly the confusion the disclaimer is trying to prevent. RECORD §4.2's line is editorial ("*Decided (Byron, September 14, 2026), on the strength of the above:* INTERVAL is **35 ms**, and no problem overrides it").
  - *fix:* Drop the quotation marks and write it as the record's statement of his decision: *Byron, September 14, 2026, deciding it on the epoch sweep [RECORD §4.2]; the record states the decision, it does not quote him.*
- **out-of-scope** — §2.3 "A network may instead be built with one fixed weight given to every connection; that is a test's network, not a run's"
  - The second half is not in the record and is not what the code does. RECORD §3.3 says only "unless a fixed weight is given to all"; `--weight` is a command-line option that reaches every container constructor on a run (cli.py:937, 984, 1020, 1404). The clause legislates a restriction nobody decided, which cuts against the no-artificial-restrictions value of RECORD §2.
  - *fix:* Keep the record's form: every weight is drawn uniformly from WEIGHT_RANGE unless a run gives one fixed weight to all.
- **wrong-rule** — §2.4 "$w_{ij} \leftarrow \operatorname{clip}(w_{ij} + \Delta, -1, +1)$"
  - Hardcodes the default range into the formula while the sentence around it says the bound is WEIGHT_RANGE. Under `--positive-weights` the range is [WEIGHT_EPSILON, 1] (RECORD §1.1), and every engine clips to the network's own `weight_range` (network.py:221, learning.py:622-628).
  - *fix:* Write the clip to WEIGHT_RANGE's own ends, and give [−1, 1] as its default value in the constants clause rather than inside the rule.
- **missing** — §2.1, §2.2, §2.3, §2.4, §2.7, §3.1, §3.10 — "*Byron and Cedric [RECORD §3].*"
  - The brief requires each clause to be dated as well as attributed. Seven clauses give an attribution with no date at all ("from the beginning" is not one). The record dates this material: its header says the connectivity and signalling scaffolding was "agreed on September 10, 2026", and §3, §3.3, §4.1, §4.2 and §4.4 are all in that scope.
  - *fix:* Date them September 10, 2026 (the scaffolding agreed that day, RECORD's header and §3-§4), keeping the later corrections and their own dates where the draft already has them.
- **missing** — §2.6 "It is cleared when the target spikes and when the floor bites, the two events"
  - There is a third, and the record names it: "the **floor**, a forced spike and a **discharge** settle with no credit" (RECORD §6.7), and `Neuron.reset(discharge=True)` calls `clear_arrivals()`. `--discharge` zeroing the potentials is also the option RECORD §4.2 names in the reset step, which §3.9 leaves out entirely.
  - *fix:* Name the discharge in §2.6 as the third clearing event and in §3.9's reset step ("potentials are kept; `--discharge` zeroes them instead, and nothing is then left open on any synapse"), or say outright that the specification drops the discharge — but the scope's drop list does not mention it.

**Missing from this section:**
- "Drop the codings", confirmed against Byron's own words before drafting: docs/rewrite-answers.md, "The codings, confirmed 15:10 MDT" — KEPT are complement coding on the inputs, complement coding on the outputs, and the clock neurons ("what mnist runs"); DROPPED are raw coding, population coding, population-complement, the Hamming and parity error-correcting codes, and the input bit flips (FLIP). So it means every input/output coding except complement coding on both sides, the clock neurons surviving with it. That matches the scope given for this draft, and nothing in sections 2 or 3 turns on it.
- Scope conflict to settle before the critic clauses are drafted (it does not touch sections 2 or 3). The task text says the critics kept are "evidence, row and graded" and the fraction right is "a REPORTED MEASURE, not a critic". Byron's last answer of the day overrides both: docs/rewrite-answers.md, 15:12 MDT, "keep the class critic" — "So four critics survive: evidence, row, graded and class ... so the reported measure and the critic are one rule again rather than two." Draft four critics, not three, and do not write the fraction right as a measure separate from the class critic.
- The exploration stream has no home. §3.8 takes the wave's uniforms "from the network's exploration stream", but that stream is defined in RECORD §6.1, whose additive exploration the scope drops, and it is a third stream distinct from the network's own (weights, wiring, the drive's Poisson times) and from the input stream of §4.5. Something in this specification must say what the three streams are, who consumes each, and that the Rust loop takes Python's MT19937 state so the draws are equal and not approximately equal (RECORD §6.15, §7).
- The clock section does not say the run has no end. RECORD §7's invariant — "There is no training run and no evaluation run, only one run that keeps going; a rule may not assume an end" — belongs with §3.1's clock, and is load-bearing for the epoch clauses (why an epoch is a boundary and not a reset, why a debit may run past a read).
- §3.5 does not say a neuron may fire any number of times, the refractory period permitting, which RECORD §4.4 states and which is what makes the count read and multiple decisions an epoch possible. §3.9 and §2.9 both lean on it.
- §3.5 drops the word "active" from RECORD §4.4's "Each firing neuron's active outgoing connections are scheduled" (`Neuron.fire` returns only `is_active` connections; the array and Rust engines carry the flag too). Either keep the flag as a clause or say the specification drops it — silently losing it will lose it in the rebuild.
- Nowhere in these two sections is the ISI factor named, though it is on by default and weighs every charge §2.8 and §2.9 describe. §2.8's $E_j$ and §2.9's re-basing are both ISI-weighted quantities. If the factor's clause lives in the learning section, §2.8 and §2.9 should point at it so the two are never read as unweighted.

**Choices the drafter had to make:**
- "Drop the codings" (Byron, September 17, 15:10 MDT) reads as: raw, population and population-complement coding, the Hamming and parity error-correcting codes and the input flips (FLIP) leave the specification; complement coding on the inputs, complement coding on the outputs and the clock neurons stay, because that is what mnist runs. Neither the synapse nor the clock states a coding, so no clause of my part turns on it, and none was written for it.
- The record gives the clock's tolerance as "times are rounded to a nanosecond"; what is built in all three engines is a relative slack, TOLERANCE x max(1, |t|) with TOLERANCE = 1e-9. A clause has to be implementable, so 3.4 states the slack and says the record's phrase is the same rule under a millisecond. Marked as Claude's reading.
- The record's epoch has five steps, the third being a per-epoch exploration nudge of every potential (the perturb rule, dropped). Clause 3.9 has four steps, and 3.8 names where the wave's per-neuron draw is taken instead.
- Clause 3.6 makes the order the fire phase asks in normative (forced, then the touched in the order touched, then every other neuron in index order). The record states only the push-order rule; the decision order is what fixes push order, so it must be a clause. Marked as Claude's reading.
- Clause 3.5 says every neuron that is not refractory decides in every wave, not only the ones the wave touched, since that is what lets a neuron nobody talks to spike. What the decision is stays with the firing clauses.
- Clause 2.4 states the clip once, as a rule on every update, rather than repeating it inside each learning clause.
- Clause 2.9 names the re-basing of the note at the read (B_ij <- x_ij E_j), which the record leaves as "the open arrivals counting from now" and all three engines do. Marked as Claude's reading.
- Clause 2.3 keeps the fixed-weight build from RECORD 3.3 but says plainly it is a test's network, not a run's, so it cannot be read as a second way to start a run.
- Clause 2.5 lists the synapse's state exhaustively (four pieces) so an engine cannot quietly add a fifth; the leak option adds the moment the trace was brought up to date, which 2.6 names as part of the trace.

**Left open for Byron:**
- INTERVAL: the default is 35 ms, but every mnist run the specification states ran at 100 ms, set from the command line. Byron to say whether the specified default is now 100 ms (named in clause 3.11).
- --discharge (the epoch's reset zeroing potentials instead of keeping them) is in neither the kept nor the dropped list, and the learning rule's settling clause names a discharge as an event that settles with no credit. Keep it as a non-default, or drop it and delete that arm of the settling rule.
- WEIGHT_EPSILON and --positive-weights (WEIGHT_RANGE narrowed to [epsilon, 1]: a network with no inhibition) are named nowhere in the kept or dropped lists. Not written as a clause; Byron to keep or drop.
- A connection's is_active flag and its kind label (local / local2 / small_world / goo) have no live use once the dopamine gate, the direct cut and the grid go. Clause 2.5 says the synapse carries four pieces of state and nothing else, which drops both; confirm.
- The score e_ij is not in the checkpoint (the trace, the note and the stamp are), so a round-trip is exact only for a checkpoint taken at a read boundary. Either the checkpoint carries the score or the invariants clause says checkpoints are taken at a read.
- Cross-references: clauses 2.6, 3.5 and 3.8 point at the neuron, firing, drive and learning clauses by name, and 3.7 at the platform clause. They need numbers once the sections are assembled.
- Clause 3.7 (push order is normative) and the array engine's part-in-a-billion exception are stated here because the wave is where the summation happens; if the platform section states the tolerance, 3.7 should cite it rather than restate it.
- Ordering: section 2 uses "integrated", "wave" and "refractory", which section 3 and the neuron clauses define. Consider putting the clock before the synapse, or accept the forward references as written.

## container (§4) — fixable

- **wrong-attribution** — 4.9 A wiring that was drawn needs its seed to be rebuilt
  - The words in quotation marks — "a goo wired below 1 needs its seed to be rebuilt, as a grid needs its seed for its shortcuts" — are not Byron's. In RECORD.md §3.4 ("The wiring is a rule") they are the record's own prose following Byron's quoted rule: "At $P = 1$ nothing is drawn for the topology and $N$ fixes it; below 1 the seed's stream decides, pair by pair in $(i, j)$ order, the projection draw and then the weight, and a goo wired below 1 needs its seed to be rebuilt, as a grid needs its seed for its shortcuts." The record marks everything in that section below Byron's rule as Claude's reading. The date (September 14, 2026) is right; the attribution as a quotation is not.
  - *fix:* Drop the quotation marks and attribute it as the record does: the seed rule is Claude's reading of Byron's probabilistic wiring of September 14, 2026, written down so that changing it is editing a working system. Quote Byron only where he decided something.
- **wrong-rule** — 4.9 A wiring that was drawn needs its seed to be rebuilt
  - "Where no probability was strictly between 0 and 1 — ff2, or a scaled goo whose every probability is 0 or 1 — ... it rebuilds with no seed at all" is not what the code does for ff2. In src/walnutbutter/persistence.py `_restore_goo`, `drawn` is computed for every non-scaled wiring as `data.get("projection", 1.0) < 1.0`, and save_checkpoint always writes `data["projection"] = grid.projection`, which for an ff2 goo is whatever GOO_PROJECTION was (0.2 by default) since ff2 ignores it. So an ff2 checkpoint with no seed is refused today, contrary to the clause.
  - *fix:* Either state the rule as drafted and say under "what this requires of the engines" that whether a wiring drew anything is decided from the wiring and its own knob, not from a stored probability the wiring does not read (the code then has a bug to fix, the file winning) — or restrict the clause to ff2-partial at P = 1 and the scaled goo. Naming it is required because the clause is the only place a rebuild is specified.
- **wrong-rule** — 4.3 Zones by index
  - "Being in a zone says where a neuron is driven and where it is read, and nothing else follows from it: no rule branches on a neuron's index or its zone" contradicts 4.5-4.7 of this same draft, where the wiring branches on zone at every case, and it overstates RECORD.md §0, which says only: "An 'input' neuron, a 'hidden' neuron, and an 'output' neuron are defined only with respect to where external connections exist. The neurons themselves operate identically."
  - *fix:* Restrict the sentence to the neuron's own operation: no rule of a neuron's integration, firing decision or learning branches on its index or its zone; the wiring (4.5-4.7), the drive and the read do, and that is what a zone is for.
- **wrong-attribution** — 4.3 Zones by index
  - The permissive value is cited as "the standing value of §0". In RECORD.md it is the last line of §2 (line 382): "A neuron may sit in several input and output zones at once; structures are permissive, never artificially restricted." §0 (the Strong Statement) does not contain it. The clause's own bracket already says [RECORD §2, §3.4], so the in-text "§0" also reads as a forward reference to a section of the new file that does not exist yet.
  - *fix:* Cite RECORD §2 for the quoted value, or, if the intention is to point at the new file's own values section, write it as a forward reference that is filled in once that section is numbered.
- **wrong-rule** — 4.10 The potential axis scales with fan-in
  - The clause gives GOO_THRESHOLD = 0.2 and GOO_MINIMUM_POTENTIAL = -0.8 as two free constants, then says the floor moves with the threshold "because it is not a second decision". In the code the floor is derived, not quoted: constants.py has GOO_MINIMUM_POTENTIAL = GOO_THRESHOLD * MINIMUM_POTENTIAL / THRESHOLD, and RECORD.md §1.2 states it the same way ("goo's floor, GOO_THRESHOLD × MINIMUM_POTENTIAL / THRESHOLD: the grid's ratio of −4"). As drafted, a later change to GOO_THRESHOLD would leave the floor where it was, which is exactly the second decision the clause says it is not.
  - *fix:* State the ratio as the rule and the number as its value: the floor is -4 x GOO_THRESHOLD, so GOO_MINIMUM_POTENTIAL = -0.8 at GOO_THRESHOLD = 0.2, and it follows the threshold if that constant moves.
- **missing** — 4.7 ff2-partial — the same two layers at a probability
  - "GOO_PROJECTION = 0.2" is stated as this wiring's default with nothing said about where the value came from or that it is open. RECORD.md §1.2 marks GOO_PROJECTION as "superseded as the wiring's knob by GOO_SCALING_FACTOR, September 16, 2026" and sets 0.2 from Byron's September 15 sweeps of the zone rule — a wiring the scope drops. The runs the specification is meant to state ran ff2-partial at P 0.25 and 0.5 (RECORD §8), not 0.2. So the clause quietly carries a value chosen for a dropped wiring as the default of a kept one.
  - *fix:* Say in the clause that 0.2 is the constant's inherited value, set for a wiring that has left the specification, and that the value for ff2-partial is open and Byron's; cite [RECORD §1.2, §8] for provenance without importing the sweep numbers.
- **wrong-attribution** — 4.4 A projection is an ordered pair
  - The rule the clause states — one way, an independent weight each direction, no self-projection, ids from 1 in the order they are made — is RECORD.md §3's standing rule, which predates September 14, 2026 ("Every connection is one-way, from a source to a target, with its own weight... No neuron connects to itself. Connections have ids from 1 in the order they are made"). The clause instead attributes it all to Byron's September 14 quotation, and that quotation is the superseded zone rule ("0 if i in inputs or outputs AND j in inputs or outputs"), whose zone clause this specification drops. A reader of both files meets a dropped rule quoted inside a clause that does not state it.
  - *fix:* Attribute the ordered-pair rule to RECORD §3, and quote from Byron only the verb correction he made that day: "Instead of 'connects' I should have said 'projects'. The connections are all one-way." Leave the zone-rule quotation to 4.5's provenance, where the scaled rule that replaced it lives.
- **out-of-scope** — 4.1 Goo is the only container
  - "they were the only thing that shortened a trip across a grid, and goo has no far" is an argument for the shortcuts carried over from RECORD.md §3.4 ("What it is for") and from the prose of docs/rewrite-answers.md, not from Byron's words. His decision is only: "archive the hex containers, lattice, spread, the whole shebang. We'll bring back small world shortcuts later." The rewrite's own rule is that the reasons stay in the record.
  - *fix:* Name the intention and stop: small-world shortcuts are to return, not a clause, and no rule below may assume them. Cite [RECORD §3.2, §3.4] if provenance for the mechanism is wanted.
- **style** — 4.3 Zones by index
  - "The zones are addressed the way every other container's rows were, so the drive and the read need not know what container they are on" is history of containers this specification has just dropped (4.1). With goo alone there is no other container for the interface to be shared with, and the sentence reads as a rule about containers that no longer exist.
  - *fix:* State the interface plainly as goo's: get_neuron_at(place, 1) is the input zone, get_neuron_at(place, 0) the output zone, rows = 2 counting the two zones and no depth, which is what the drive and the read address. Drop the comparison.
- **style** — 4.5 The scaled rule — the default wiring
  - The zone-overlap refusal is stated twice, in 4.3 ("the count must leave room for both") and again in 4.5 ("The zones may not overlap — the count must be at least the two widths together — and a goo that would overlap them is refused"), against the one-rule-to-a-clause instruction. The code refuses overlap once, for every wiring, in Goo.__init__ ("the zones would overlap: count must be at least the zones together"), independently of which wiring is chosen.
  - *fix:* Keep the refusal in 4.3, where the zones are defined, since it holds for every wiring; leave 4.5 with only what is particular to the scaled rule (that it needs no interior, unlike the wirings that have left the specification).
- **style** — 4.10 The potential axis scales with fan-in
  - "The floor moves with the threshold because it is not a second decision: the two are points on one axis and it is the axis being rescaled" is the record's argument for the rule (RECORD §5.2, "The floor moves because it is not a second decision"), and the sentence that follows it there is the saturation measurement the draft rightly leaves out. As drafted the clause keeps the argument without the measurement, which is the half the rewrite says belongs in the record.
  - *fix:* State the rule (both points of the axis are rescaled by the same factor) and cite [RECORD §5.2]; leave the reasoning there.

**Missing from this section:**
- The weight draw itself. 4.8 says a weight is "one uniform on WEIGHT_RANGE" but no clause in this part states WEIGHT_RANGE = [-1, 1] or that learning clips every weight back inside it (RECORD §1.1 and §3.3, "Weights", which the scope keeps). Either give it a clause here, beside the wiring that draws it, or say which section carries it.
- What the seed's stream does after the wiring. 4.8 stops at "nothing else of the topology is drawn", but the input permutation that used to sit next on the stream is dropped, so the order that now follows (the drive's arrival draws, then the one uniform per neuron per wave of the firing decision, RECORD §5.2 and §7's "A seed is the whole run") is what makes the three engines agree from one seed. Name the order here or in the input-stream clause, and say the two must agree.
- The option to turn the fan-in scaling off. 4.10 states the rescaling unconditionally, but it is a per-run switch in the code (Goo's scale_with_fan_in, stored in the checkpoint as scale_with_fan_in, RECORD §5.2's --scale-with-fan-in) and a checkpoint restores under the setting it was saved with. Either say it is always on for goo now, or name it as a per-run option the way the leak (TAU) is named.
- The fixed-weight run. 4.8 allows that "the run fixes every weight instead" but does not say that no weight is then drawn, so a fixed-weight goo and a random-weight goo consume the seed's stream differently (Goo._wire draws only when weight is None). Every engine has to agree on that, so the clause should say it.
- How the input zone's width is made up. 4.3 defines `across` as a width only. The runs the specification states put the clock neurons inside it (RECORD §8: "three clock neurons ... the 196 on-off pixels, and the 196 complement-coded pixels — 395 input neurons, no permutation"), and the clock neurons and complement coding are both kept by the scope. A pointer from 4.3 to the coding clause would stop the two sections being read as giving two different widths.

**Choices the drafter had to make:**
- Put the fan-in scaling of the potential axis in §4 (the record has it in §5.2) because it is what the container hands each neuron at build time; the firing section then uses theta_j and quotes the escape width against it. Moving it back is a renumbering, not a rewrite.
- Wrote both the permissive value (a neuron may sit in several zones at once) and the wirings' refusal of overlapping zones, side by side, rather than choosing between them, and named the tension as open — the record permits overlap for goo as first built and every surviving wiring forbids it.
- Quoted GOO_MINIMUM_POTENTIAL as -0.8 outright rather than as GOO_THRESHOLD * MINIMUM_POTENTIAL / THRESHOLD, since the grid's THRESHOLD and MINIMUM_POTENTIAL leave the specification with the grid; the -4 ratio is stated in words instead.
- Called a neuron in neither zone a hidden neuron throughout; the record uses "interior" and "hidden" for the same neurons, and "interior" belongs to the zone wirings that are dropped.
- Stated ff2-partial at P = 1 equalling ff2 "to the bit" as a requirement on the engines rather than as an observation, since it is what the test holds and what the shared stream ordering guarantees.
- Made the draw order a clause about the seed's stream (4.8) that also says what it requires of the engines: the topology is drawn once in the container and copied, so no tolerance applies to the wiring or the starting weights.
- Read GOO_PROJECTION's surviving role as ff2-partial's P alone, since zones, zones-equal and uniform are dropped; its range is stated as (0, 1] as the code refuses anything else.
- Kept the row addressing (rows = 2, row 1 in, row 0 out) as the container's interface to the drive and the read, rather than inventing a goo-only address, and listed it as open.
- Left ff2's and ff2-partial's refusal of hidden neurons as a hard refusal with the reason named, since the record states the generalisation to a recurrent goo is Byron's and not built.

**Left open for Byron:**
- "Drop the codings" is confirmed as Byron's answer of September 17, 15:10 MDT and changes nothing in §4: complement coding on the inputs, complement coding on the outputs and the clock neurons are kept, and raw, population, population-complement, Hamming, parity and FLIP are dropped, so the input zone's width in 4.3 is the coded width and never a POPULATION multiple.
- Whether an output neuron may project onto the input zone: 4.5's third line is Claude's reading of "no cycles" and "feedforward", to be corrected in a word.
- How ff2's two "layers" generalise to the zones of a recurrent goo is Byron's to say and is not built; until then both two-layer wirings refuse a goo with hidden neurons (4.6, 4.7).
- The permissive value says a neuron may sit in several input and output zones at once, while all three wirings require the zones disjoint (4.3, 4.5-4.7); whether the permissive reading returns is Byron's.
- The fan-in scaling is linear and "decided for now" (4.10): a different slope, or a threshold quoted against something other than the fan-in, is open.
- THRESHOLD_FAN_IN = 18 is quoted against an interior hex cell's two rings, a container that has now left the specification; whether to requote the unit against goo is open.
- The names GOO_THRESHOLD, GOO_MINIMUM_POTENTIAL and GOO_PROJECTION carry a container distinction and three wirings that no longer exist; whether they keep their names with goo alone is open.
- Whether GOO_COUNT = 60 stays a default now that every run in scope takes its count from the problem's zone widths (4.2).
- The row addressing of 4.3 (rows = 2, row 1 in and row 0 out, get_neuron_at) is a grid word kept so the drive and the read need not know the container; whether goo keeps it is open.
- The input permutation is dropped from scope, so the coded bits sit on the input zone in index order and the seed's stream after the wiring goes straight to the drive; the drive-and-read section has to state this, since §4 no longer spends the stream on a shuffle.

## drive (§5) — fixable

- **wrong-attribution** — 5.6
  - The sentence "the drive is specified by its CV, and the default is CV = 0.6" is given inside quotation marks as Byron's words. It is not a quotation: RECORD §4.3 has it as the record's own decision line — "**Decided (Byron, September 14, 2026): the drive is specified by its CV, and the default is CV = 0.6.**" No quoted words of Byron's survive for this decision. A file whose rule is "quoted where they decided it" cannot invent a quotation.
  - *fix:* Drop the quotation marks: *Decided by Byron, September 14, 2026 [RECORD §4.3].* If a quotation is wanted, use one he actually spoke; the record has none for this decision.
- **out-of-scope** — 5.8
  - "the record leaves that open in three forms" imports a context the scope drops. RECORD §6.7's three forms are (a) the rule as written, (b) "lift the skip under `read = "again"` and take e_j = ±1 by whether j refired", (c) "REINFORCE on §6.9's trace" — i.e. the `again` read, the wrong_hebb ±1 rule and the external teacher, all three dropped. The question was posed for problems that read the input zone ("Measured on population_denoise"), every one of which is dropped; under mnist no read sees the input zone, so nothing of the openness as the record states it survives into the specification.
  - *fix:* State the rule as it runs and cite it: the update skips every synapse whose target was driven this epoch, "because 'its firing was not the network's doing'" [RECORD §6.7]. Either leave openness out of §5 entirely, or name only what is still open under the kept scope, in Byron's words if any exist.
- **wrong-rule** — 5.8
  - "the update as it ran passes over every synapse whose target was driven this epoch" is ambiguous in the one direction that matters: "passes over" reads as both "skips" and "goes over each of". The record uses the same phrase, but the code is unambiguous — `if self.forced[target] { continue; }` in every update of rust/src/lib.rs, and `mask = (self.score != 0.0) & ~self.forced[self.target]` in arrays.py — the synapse is skipped. A rebuild written from this sentence can invert the rule.
  - *fix:* Write "the update skips every synapse whose target was driven this epoch".
- **wrong-rule** — 5.11
  - P is defined two incompatible ways in one clause: "a population of $P$ neurons a class" and "the zone is $2CP$ neurons" with "class $k$ owning the $P$ neurons from $kP$". In the record P is the population per half — RECORD §8 decision 6: "60 neurons, the ten fire-if-one populations of three first (class k owns 3k to 3k+2) and then, in the same order, the ten fire-if-zero populations of three", and the problem's `population` is 3 — while Byron's quoted words in the same clause say "a population of six neurons: three fire-if-one and three fire-if-zero", i.e. six a class. Read the clause's own phrase and C=10, P=6 builds 120 neurons; read the formula and P=3 builds 60.
  - *fix:* Say it per half: "a population of P neurons in each half — P fire-if-one and P fire-if-zero a class, 2CP in all; mnist's P is 3, so ten classes give 60 neurons."
- **wrong-rule** — 5.10
  - The clause sets the read at "at least TEACHER_THRESHOLD" and the engine note hardens it to "the comparison is ≥". The record and Byron's own word are strict: RECORD §1.2, "an output is on if the rate estimated from its spike count this epoch **exceeds** this", and the quotation the clause itself carries — "If the firing rate estimate exceeds TEACHER_THRESHOLD, the output neuron is 1." The code takes the other side (`network.py`: `return [level >= self.teacher_threshold for level in self.output_counts_hz()]`). The draft silently picks the code over the record in the file that is meant to decide which is right.
  - *fix:* Decide it in the clause and say so: e.g. "the comparison is ≥; the record and Byron's line say 'exceeds', and at 14.3 Hz the two never differ because no count lands on the line (28.6 Hz at one spike in a 35 ms epoch, 10 and 20 Hz at 100 ms) — the engines compare ≥."
- **missing** — 5.10
  - "The line is quoted per second and holds at every epoch length" is Claude's reading presented as settled, and the record records a doubt about exactly this. Byron set 14.3 Hz to mean one spike at a 35 ms epoch (RECORD §1.2, §4.3); the epoch-length sweep in §8 records that the reward and the read "are not comparable across epoch lengths", and names as untried "the count read as a rate, so that the critic sees the same odds at every length". The consequence the note draws — that at 100 ms the same constant silently demands two spikes — is a change in what "on" means, not a corollary.
  - *fix:* Mark it as Claude's reading, to be corrected in a word, and name what is open: whether TEACHER_THRESHOLD is a rate that holds at any epoch length or a line set to mean one spike at 35 ms [RECORD §1.2, §8].
- **wrong-rule** — 5.2
  - "Place $i$ of the input zone shows coded bit $i$" is false whenever there are clock neurons, which mnist has: 5.3 puts them first and the coded pattern is "the clock neurons' 1s followed by the complement-coded bits" (mnist.py: `np.concatenate([np.ones((len(bits), clock)), bits, ~bits], axis=1)`). Place 0 shows no coded bit at all. The same clause's "exactly half the coded input is driven" is likewise true of the complement-coded part alone: with c clock neurons the driven fraction of the zone is (c + k)/(c + 2k).
  - *fix:* Index against the coded pattern of 5.3: "place $i$ of the input zone shows place $i$ of the coded pattern (5.3), and there is no permutation"; and qualify the half — "exactly half of the complement-coded bits are driven whatever the raw bits are".
- **wrong-attribution** — 5.1
  - "Byron and Cedric, from the beginning [RECORD §4.3]" attributes and cites the wrong place. The rule is §0's Strong Statement — "The inputs are the outputs. An 'input' neuron, a 'hidden' neuron, and an 'output' neuron are defined only with respect to where external connections exist. The neurons themselves operate identically" — and RECORD §4.3 merely applies it ("Per §0 the neurons are the same everywhere; only the external connections differ"). The record nowhere attributes the Strong Statement jointly to Byron and Cedric with a date, so the clause states an attribution the record does not carry.
  - *fix:* Cite [RECORD §0] and attribute it as the record does — the Strong Statement, non-negotiable — rather than asserting a joint authorship and a date the record does not give.
- **wrong-attribution** — 5.2
  - The quoted definition ("An input is complement-coded and presented on the list of input neurons. The desired output is exactly the input expressed across the list of output neurons.") is Byron defining the **copy** problem (RECORD §8, copy, September 14, 2026 — "Let's define this task very clearly: ..."), a problem this scope drops. The words are quoted accurately, but they are made to carry a general coding rule they were not spoken about.
  - *fix:* Say what they were: "Byron, September 14, 2026, defining the copy task [RECORD §8]", and rest the clause on the coding decision of September 17, 15:10 MDT, which is the one that makes complement coding the only coding.
- **style** — 5.11
  - "The label code the read is scored against" is wider than the record. RECORD §8 decision 6 says "The label code **for the row critic** is the label's one-group on, its zero-group off, and every other class the other way." mnist's own critic reads the class sums, not a code (§5.12 says as much), so no read is scored against the label code on the kept problem.
  - *fix:* Name the consumer: "the label code the row critic scores against is ...", leaving the evidence of 5.12 to the critics.
- **wrong-rule** — 5.4
  - "until a draw falls at or past the horizon" names the wrong bound. The schedule's bound is the epoch's moment plus INTERVAL — `end = self.time + self.interval` in `Network.input_schedule` — while the horizon is whatever `fire_input(until=...)` sets and need not be the same instant.
  - *fix:* Write "at or past $t_e + \text{INTERVAL}$" (and keep "that last arrival is drawn and discarded", which the code does).
- **style** — 5.6, 5.7, 5.9
  - Three engine notes carry the record's arguments into the specification, which the drafting rule forbids: 5.7's "a Poisson process has independent increments, so restarting at $t_e$ is a continuation and not a seam" is the record's rebuttal of a claim Claude made and withdrew (RECORD §4.3, "contrary to what Claude claimed when first raising this") — history as well as argument; 5.9's "which is what makes two runs comparable epoch by epoch and not only on average" is §4.5's justification; 5.6's "the dead time of 5.5 is the only thing making the train regular, so fixing one fixes the other" is §4.3's derivation.
  - *fix:* Keep the rules (regenerate the process from $t_e$ each epoch; the input stream is drawn from its own seeded stream; λ is derived from INPUT_CV and never stored twice) and cut the reasons to a bracketed citation [RECORD §4.3, §4.5].
- **missing** — 5.5
  - "it adds nothing to the potential" is right and incomplete about the state the drive leaves behind. The driven spike goes through the ordinary fire path, which zeroes the potential (`neuron.py`: `self.potential = 0.0  # the spike resets the potential`), so under the evidence accumulator every arrival accumulated since the neuron's last spike is discarded by a driven spike. §5 is where the drive's effect on the neuron is described, and a reader of this clause alone would not know it.
  - *fix:* Add a sentence, or a pointer: an arrival that fires the neuron resets its potential as any spike does (§1), so the evidence accumulated since its last spike is discharged.

**Missing from this section:**
- Whether zones may overlap. 5.1 says a zone is where the external connections are, and 5.3 makes clock neurons an explicit exception ('They are inputs and not outputs'), which implies a neuron may otherwise be in both zones; the design value of no artificial restrictions points the same way. The section never says, and the rebuild has to know.
- Where a dataset's raw bits come from. 5.9 defines a dataset as 'a stream of the same kind' (raw-bit patterns) with labels, but mnist's images become bits by a rule: 28 x 28 downsampled to 14 x 14 blocks, 'a block on iff its mean intensity is at least half of full', 196 raw bits (src/walnutbutter/mnist.py; RECORD §8). Either 5.9 names it or it points at the problem clause (§10) that must.
- What §5 state a checkpoint keeps and a resume restores, since the scope keeps both. persistence.py stores the coding, the output coding, the clock count, the drive, INPUT_RATE/INPUT_RATE_OFF, the input-zone cells and the output width; nothing in the section says a resumed run keeps the drive and the coding it was saved under, nor where it stands in its input stream.
- The epoch's length. 5.4 draws to t_e + INTERVAL and 5.10 divides by INTERVAL, but neither names where INTERVAL is fixed, and RECORD §1.2 has it at 35 ms with 'no problem overrides it' while the mnist runs the read is quoted against ran at 100 ms. §5 should cite the clause that sets it, and the record's 100 ms mnist epochs need to be reconciled with that line somewhere.
- A note carried over from the answers file rather than the draft: the computed scope given to me says the critics kept are 'evidence, row and graded' and the fraction right is a reported measure and not a critic, but docs/rewrite-answers.md ends later (15:12 MDT) with 'keep the class critic' — four critics, and the fraction right is the class critic's own number, 'so the reported measure and the critic are one rule again rather than two'. §5 does not depend on it, but 5.12's hand-off to §9 will.

**Choices the drafter had to make:**
- Kept the drive and the read as one section (§5, thirteen clauses) rather than splitting them across §5 and §6, since the outline reserves §6 for firing and a split would renumber every other section's draft.
- Stated complement coding positively -- "complement-coded, and coded no other way" (5.2) -- instead of listing the dropped codings, the error-correcting codes and FLIP, which by the drop rule may not appear as clauses. The same device carries the drop of FLIP: "the pattern presented is the pattern the read is scored against".
- Wrote the output zone's layout generally, in C classes and a population of P (5.11), and left mnist's ten classes, population three and sixty outputs to §10, so §5 states a rule rather than a task.
- Put the driven mark in the drive (5.8) rather than leave it unstated, because the engines must record it, and named it as Claude's reading with its use open in §8.
- Named the read's comparison as >= (the engines' bit agreement depends on it) where the record's words are "exceeds", and spelled out what count the line means at 35 ms and at 100 ms, since the mnist runs used both.
- Made the arrival-draw order a rule (5.4): place by place in place order, the whole epoch drawn before the schedule runs, the overshooting draw drawn and discarded, a zero-rate place drawing nothing. None of this is obvious, and all three engines' agreement on a seed rests on it.
- Stated the per-epoch regeneration of the Poisson process from t_e as an engine requirement (5.7) with the independent-increments reason in one clause, leaving the record's simulation of it out.
- Named the deterministic spike every TARGET_ISI as an intention in a closing italic note, not as a clause, per Byron's "include the Poisson drive as it ran".
- Left the fraction right, the critics, the epoch's length, the refractory period and the firing rule itself to their own sections, citing them rather than restating them.

**Left open for Byron:**
- 5.13: whether a driven input neuron carries the firing rule's rest between arrivals is Byron's to call (RECORD §8, September 16, 2026); until he calls it §6 applies to the input zone unchanged.
- 5.8: what the learning rule does with the driven mark. The update as it ran skips every synapse whose target was driven this epoch, and RECORD §6.7 leaves three forms open; §8 settles it, not §5.
- 5.12: the class evidence n_k = n_k+ - n_k- is Claude's reading of "change our scoring rule accordingly" (Byron, September 16, 2026), to be corrected in a word.
- 5.11: complement coding on the outputs is kept in Byron's own words "for now" (September 16, 2026, 16:20 MDT); whether the fire-if-one and fire-if-zero halves ever separate is not a clause.
- 5.10: TEACHER_THRESHOLD is quoted in hertz and holds at every epoch length, so the count the line demands changes with the length (at least one spike at 35 ms, at least two at 100 ms). Byron has not said whether the line should move with the epoch instead.
- 5.1, 5.11: what a problem may set here -- the zone widths, the clock count, the classes and the population -- is stated as a fact of §5 but the general question of what a problem may set (and whether it may set the read's line or the drive) belongs to §10 and is unsettled.
- Cross-references use the outline's section numbers (§0.1, §1, §3, §4, §6, §8, §9, §10). They need checking once the whole file is assembled.
- §5 fixes no epoch length and no refractory period: it reads INTERVAL from the clock section and REFRACTORY from the neuron's, and both enter 5.6 and 5.10 directly.

## firing (§6) — fixable

- **wrong-rule** — §6.10 A forced neuron fires by its stimulus and decides nothing
  - "so it charges no credit and no expectation to its synapses for that spike" is wrong on the second half. RECORD §6.7 groups the forced spike with the floor and the discharge: "the **spike** settles every open arrival, e_ij += c_j x_ij - (x_ij E_j - B_ij) ... the **floor**, a forced spike and a discharge settle with no credit". The code agrees: fire() calls settle_arrivals(self.credit) with self.credit = 0.0 at a forced spike (neuron.py:328, 185-199), so every open arrival is closed with credit 0 but its accrued debit -(x_ij E_j - B_ij) IS charged. Only the credit is withheld; the expectation is settled exactly as at the floor.
  - *fix:* "...and makes no firing decision that wave, so no credit is charged for that spike; the arrivals it closes settle their debit as at the floor (§6.3)."
- **wrong-rule** — §6.11 The spike resets the neuron and is remembered
  - "the gap between them is what its own rules read as its interspike interval (§7.4)" misstates §7.4. The ISI factor reads the time since the neuron's own last spike, measured at the decision — isi_factor(now, self.fired_at, TARGET_ISI) (neuron.py:11-20, 295), RECORD §0.2: "t is the time since the neuron's own last spike" — not the gap t_fired - t_prev. Under the specified scope t_prev is read only by the quash (the delay since the previous spike, §6.11 of the record, a non-default), so §7.4 is the wrong cross-reference for it.
  - *fix:* Keep the reset and the two remembered times; say that t_fired is what §7.4 measures from at a decision, and that t_prev is read by the quash (§8, non-default). Do not call the gap the interspike interval the rules read.
- **out-of-scope** — §6.8 Every neuron decides at every wave, touched or not
  - The attribution smuggles in a measurement: "measured to be about 3,300 decisions an epoch on the mnist goo, which is what DECISION_MEMORY is quoted against". AUTHORITY takes no results ("NO results, NO sweeps, NO findings") — this belongs in RECORD §1.3, which is also more exact and differently conditioned: "3,266 to 3,398 over two seeds, the scaled and ff2-partial wirings, TAU 2 and ∞", and at a 100 ms epoch, not the specification's INTERVAL of 35 ms. As written the clause both breaks the rule and mis-cites the number.
  - *fix:* Delete the measurement. If the clause needs the consequence at all: "every decision is a charge of the single-spike rule (§8), and a neuron's expectation of itself moves per decision and not per epoch [RECORD §1.3]."
- **out-of-scope** — §6.5 The hazard and the chance of a spike
  - "the hazard is charged for elapsed time rather than tossed per wave so that a neuron's rate does not depend on how busy its neighbours are" is an argument for the rule, not the rule, and it is Claude's: RECORD §5.2 carries it under "*Why a rate and not a coin per wave (Claude).*" The clause's attribution names only Byron, September 15.
  - *fix:* Cut the because-clause (it is the record's, at §5.2). Keep the rule: Δt is the time since the previous decision, or since the refractory period ended.
- **wrong-attribution** — §6.3 A neuron at the floor has nothing of any synapse left in its potential
  - Attributed to "Byron's single-spike rule, September 17, 2026, and ... the hazard eligibility of September 15, 2026". Both of those are Claude's derivations on Byron's decisions: RECORD §6.7 has "*Claude's derivation, from [1], asked for that morning*" for the hazard eligibility, and for the single-spike rule "Claude's derivation ... and Byron's three decisions on what it left open". The trace-zeroing at the floor is in the derivation, not in any of Byron's three decisions.
  - *fix:* "Claude's derivation of September 15 and 17, 2026, on Byron's decision to derive the rule; to be corrected in a word. [RECORD §6.7]"
- **wrong-attribution** — §6.8 Every neuron decides at every wave, touched or not
  - "Stated in the record with the bored neuron, September 12, 2026" leaves out that it is Claude's reading. RECORD §5.4 gives Byron's decision as the homeostasis instruction and then marks the mechanism "*Claude's reading, built:*", under which "Every wave now checks every neuron, touched or not". The specification is required to say where a rule is Claude's reading of an instruction.
  - *fix:* "Claude's reading, built September 12, 2026, under Byron's boredom decision, and carried into the hazard on September 15; to be corrected in a word. [RECORD §5.2, §5.4]"
- **missing** — §6.2 The floor applies once a wave, to the wave's total; §6.11 The spike resets the neuron
  - Neither clause is dated or attributed. §6.2 gives only "its place in the wave is the record's wave"; §6.11 gives "The record's firing rule, standing since the project began". AUTHORITY's own rule is "each clause numbered, dated and attributed".
  - *fix:* Both descend from §0, which Byron and Cedric own: date them to the project's beginning and name them, as §6.12 does. The floor's once-a-wave placement is the record's wave of §4.4 and should say whose reading it is.
- **missing** — §7.3 One stream, one order, in every engine
  - "one seeded stream, distinct from the input stream" names only two of the three streams and invites a rebuilder to merge the exploration stream with the network's own. The escape uniforms come from `explore_rng` (the Teacher's `random.Random`, learning.py:775, network.py:617); the network's own `_rng` (goo.py:192) draws the wiring, the weights and — RECORD §4.5 — the drive's Poisson arrival times (`self._rng.expovariate`, network.py:483); the input stream of §4.5 draws the patterns.
  - *fix:* "...one seeded stream of its own, distinct both from the input stream that draws the epoch's patterns and from the network's own stream, which draws the wiring, the weights and the drive's arrival times (§4.5)."
- **missing** — §6.4 The margin, and the width of the decision
  - The clause says the widths are set once from the starting thresholds, but never says a checkpoint keeps them. It must: persistence.py stores `deltas`, one per neuron, and restores them (persistence.py:103, 371) precisely because θ_j may have moved since. §6.6 makes the opposite requirement for the count factor ("recomputed ... rather than stored"), so with §6.4 silent a rebuilder will recompute Δ_j from a moved threshold on resume and get a different network.
  - *fix:* Add: "Of the engines: each neuron's width is stored in a checkpoint and restored, not recomputed from the threshold the run has reached — unlike §6.6's count factor."
- **missing** — §6.1 The potential axis is quoted per unit of fan-in
  - "a container that scales" leaves it open whether the only container the specification keeps does scale. It does, by default: goo.py:128 `scale_with_fan_in: bool = True`, applied after the wiring (goo.py:199-200). With the grid, the columns and the lattice dropped, the conditional is a hole rather than a generality.
  - *fix:* Say it outright: goo scales, so under this specification every neuron's axis is scaled by d_j/F unless a run turns the scaling off.
- **missing** — §6.4 / §6.5 / §6.9, θ_j(t)
  - The clauses write a time-varying threshold θ_j(t) but nothing in §6 or §7 says what moves it within a run. In the record the within-run time dependence was the bored clock's falling threshold (§5.4, BORED_AFTER, `Neuron.threshold_at`), which the scope drops. Left as it stands, a rebuilder reading θ_j(t) will build `threshold_at` again.
  - *fix:* State once, in §6.4, that θ_j is constant through a run except as homeostasis and un-sticking move it between epochs (§8, non-defaults); the falling threshold of the bored clock is not in this specification.
- **style** — §6.5 The hazard and the chance of a spike
  - "$m$ is capped at $10^3$" and "$P$ is computed as $-\text{expm1}(-m)$" are correctly marked Claude's, but the third engine detail in the same formula — that Δt is clamped at zero ("and is never negative", neuron.py:251-252, arrays and Rust alike) — is stated as rule with no such mark, and it is not in RECORD §5.2 either.
  - *fix:* Move the clamp into the same sentence as the cap and the expm1 form, under the same "Claude's, required for the engines to agree; to be corrected in a word".
- **style** — §6.5 and §6.6
  - The √(N0/N) factor and the value ESCAPE_REFERENCE_COUNT = 60 are stated twice, once in each clause, against "one rule to a clause". §6.5 is the hazard; §6.6 is the count's scaling of it.
  - *fix:* Leave the factor in §6.6 and have §6.5's formula carry it as a named term with a forward reference, e.g. "the count's factor of §6.6".
- **style** — §6.1, §6.3, §6.6, §6.8, §7.5 — the forward references to §8
  - §8 is used for two different things: "§8, non-defaults" for homeostasis and un-sticking (§6.1), and "§8, the single-spike rule" / "the score of §8" for the learning rule (§6.3, §6.6, §6.8, §7.5). One of the two is wrong, or §8 is doing double duty in a way a reader cannot resolve.
  - *fix:* Fix the numbering when the learning section is drafted, and cite the clause rather than the section where the reference is to one rule (the record cites "§6.7" for the score, not "§6").
- **style** — §6.1 The potential axis is quoted per unit of fan-in
  - The clause quotes the axis in THRESHOLD and MINIMUM_POTENTIAL, which are the dropped containers' constants (0.25 and −1, the grid's), and only then gives goo's. In the code GOO_MINIMUM_POTENTIAL is *derived* from them (constants.py:41), so naming them is defensible, but with goo the only container kept a rebuilder is invited to carry two live pairs where the specification has one.
  - *fix:* Quote the rule in the container's threshold and floor, name THRESHOLD_FAN_IN = 18 as the unit, and give goo's 0.2 and −0.8 as the values; note the ratio of −4 as the container's, not the grid's.
- **style** — §7.1 The firing decision is the exploration
  - "This is Williams's Bernoulli semilinear unit with the noise in the threshold rather than on the potential — an escape-noise integrate-and-fire neuron, the spike response model's soft threshold" is an identification of the rule with the literature, not a rule. RECORD §5.2 keeps it under "*What it is*". The draft does mark it Claude's, which softens it, but it argues rather than specifies.
  - *fix:* Keep the rule (the decision is the exploration; nothing is added to any potential and no perturbation is recorded) and move the identification to a bracketed provenance line, or drop it to RECORD §5.2.

**Missing from this section:**
- The hop. §6.5's m = (Δt/hop)·√(N0/N)·e^{s/Δ} uses "hop" as its unit and no clause in §6 or §7 defines it; §6.12 only mentions "a hop of 2.5 ms" in passing. RECORD §1.2: REFRACTORY_HOPS is "the refractory period divided by the time a signal takes to travel one hop; not an integer", 2 since September 17, 2026, so hop = REFRACTORY/REFRACTORY_HOPS = 2.5 ms. State it as a clause of its own (it is also the signal delay of §4.4 and the unit the hazard is quoted in).
- The tolerance in the refractory comparison. §6.12 asserts that at two hops "a spike sent around a two-way loop returns exactly as the period ends and may refire the neuron". That holds only because the engines compare with a slack: `now + slack(now) < fired_at + REFRACTORY` (neuron.py:127, clock.py:15-16, and `slack_v` in arrays.py:49). Without it, floating-point arithmetic decides whether the refire happens. This is exactly the sort of engine requirement the section is asked to name.
- That the margin reads the potential of the run's neuron. §6.4 defines s = p_j(t) − θ_j(t) with no word on the leak, which the scope keeps as a per-run option (TAU). Under TAU finite p_j(t) is the potential decayed to the decision's moment (RECORD §5.2: "the leaked potential after the wave's signals and the floor"; `potential_at`), and the hazard then understates the charge over an interval while it is exact under the accumulator. One sentence of cross-reference to §5 would close it.
- A clause for the discharge, or a cross-reference from §6.3. RECORD §6.7 settles the floor, a forced spike and a discharge together, and `clear_arrivals` is called on a discharged neuron (neuron.py:352-353). §6.3 covers only the floor and §6.10 only the forced spike; the discharge is nowhere in §6 or §7.
- That the container's fan-in scaling is what makes §6.4's width uniform. §6.4 says the width is quoted in units of the threshold the container gave the neuron "so §6.1's scaling applies to the width as to the rest of the axis" — but the engines set Δ_j from the threshold at the moment the width is set (`set_delta`, network.py:186), which is why the order (wire, scale, then set the width) is a requirement and not an implementation choice. §6.4 states the order but not that the width is the threshold's at that moment for a neuron the scaling left at scale 1.

**Choices the drafter had to make:**
- "Drop the codings" confirmed as Byron's answer of 15:10 MDT: complement coding on inputs, complement coding on outputs and the clock neurons are kept; raw, population and population-complement coding, the Hamming and parity codes and FLIP are dropped. It touches §6 and §7 only negatively - no clause of firing or exploration branches on how a bit was coded, and none does.
- The threshold stays and is stated in full with its fan-in scaling, per Byron 14:53 MDT ("We are going to keep the level"). Deprecating theta and exploration at the synapse are carried only in the closing italic note as intentions, never as clauses.
- The record's §5.2 measurements - the saturation-edge sweep, the rest-rate and floor tables, the count-scaling table, the arithmetic on what the sqrt law holds - are all left in RECORD.md. §7.2 keeps only the rest-rate formula, which is §6.5 evaluated at s = -theta and so a statement of the rule rather than a finding.
- The bored clock is dropped, so §7.2 states the positive rule (a neuron nobody talks to fires at the hazard's rest) and quotes Byron's September 15 decision that nothing is run on top of the hazard, without giving BORED_AFTER a clause.
- The additive exploration noise is dropped with SIGMA and EXPLORE, so §7.3 says the exploration stream supplies the firing decisions "and nothing else" - the record's rule that the additive draw comes first is not carried.
- The 1e3 cap and the -expm1(-m) form are stated as engine requirements in §6.5 and attributed to Claude: the record's §5.2 gives the formula without them, and they exist so the three engines agree to the bit.
- The floor is given two clauses (§6.2 where it applies, §6.3 what it does to the synapse traces) rather than one, because they are two rules and the second is the one a learning-rule reader needs.
- §6.8 (every neuron decides at every wave) is stated as a clause of firing rather than a note, because it fixes the number of decisions an epoch and so the unit DECISION_MEMORY is quoted in.

**Left open for Byron:**
- TARGET_ISI = 5.1 ms is hardcoded and, in Byron's words, "not known"; the ISI factor's constants are the one thing §7 leaves open on its own account (RECORD §0.2, §1.3).
- Whether the ISI factor's t is the time since the neuron's own last spike, and whether "the teacher's existing reinforcement" means every per-decision charge, are Claude's readings awaiting Byron's word.
- Overlap to resolve when the sections are assembled: §6.1 (the threshold, the floor and the fan-in scaling) and §6.12 (REFRACTORY, REFRACTORY_HOPS, the hop) may belong to the neuron/clock/container sections. If those sections fix them, §6.1 and §6.12 should become cross-references, keeping only the margin, the width and the refractory neuron's non-decision here.
- §6.1 quotes THRESHOLD and MINIMUM_POTENTIAL as the pair the axis is quoted in and names goo's GOO_THRESHOLD = 0.2 and GOO_MINIMUM_POTENTIAL = -0.8 as the values in force. With goo the only container in the specification, Byron may want the constant register to carry one pair under one name rather than two; that is a naming decision, not a rule change.
- §6.11 does not say what a spike schedules (the outgoing signals one hop later) - that belongs to the clock or the wave section; check it is stated there.
- The tolerance sentence in §7.3 assumes the "one authority, all platforms" clause lives in §0; if it is numbered elsewhere, the cross-reference needs fixing.
- §6.3 and §7.5 reference the single-spike rule's trace, credit and expectation by name; they depend on the learning section stating x_ij, c_j, q_j and DECISION_MEMORY.
- §6.1's caveat that homeostasis and un-sticking move a threshold names them as non-defaults; the learning section must say so too, or the caveat reads as though they are on.

## learning (§8) — fixable

- **wrong-rule** — Preamble: "One rule moves a weight: REINFORCE"
  - False as written, and it contradicts the scope. The quash (RECORD §6.11) also moves weights — at a refire, every incoming synapse that carried a signal integrated since the previous spike moves by w ← clip(w(1 − QUASH_RATE·e^{−QUASH_K·(t−t_prev)})) — and the record says of it: "It is local, lazy, needs no external signal, and runs under every rule, so it composes with the teacher rather than replacing it". Byron kept the quash (a non-default). QUASH_RATE/QUASH_K are live in constants.py and the quash runs in all three engines (arrays.py `_quash`, the Rust loop).
  - *fix:* Write it as "One rule pays for an epoch: REINFORCE", or name the exception: the quash is the other thing that moves a weight, it composes with this rule, and it lives in its own clause (off unless a run asks for it).
- **wrong-rule** — 8.3 The default eligibility ("hebb where the threshold decides")
  - Neither the record nor the code has this default. RECORD §1.3: "The Teacher and the command line follow the neuron unless told otherwise: hazard on a network with escape noise ... and this constant when the threshold decides — Claude's reading of the default Byron set, September 15, 2026"; learning.py line 744 is `eligibility = "hazard" if getattr(grid, "hazard", False) else ELIGIBILITY`, and ELIGIBILITY is perturb. The draft silently substitutes hebb for the dropped constant, and then attributes the whole default to "the default Byron set September 15, 2026" — on September 15 hebb did not exist under that name (the single-spike hebb is September 17; the name belonged to count_hebb until then), so half the attribution is to a decision Byron never made.
  - *fix:* State the half the record supports (hazard where the firing decision is a draw, Byron's default of September 15, 2026, Claude's reading) and say the other half is OPEN in the record's manner: the constant that stood there was perturb, which leaves the specification, so which eligibility runs when the threshold decides is not decided and needs Byron's word. Do not let a new default in under a "to be corrected in a word" flag.
- **out-of-scope** — 8.13 "A late signal counts, and there is no second trace"
  - Two faults. (a) The record does not say LATE = count for these eligibilities, it says the question does not arise: RECORD §6.7, hazard — "LATE does not apply — the trace says what each signal was contributing at each decision"; count_hebb — "LATE does not apply and neither does the leaky trace". The draft's "a signal arriving after its target has already fired earns its count like any other" also reads as if such a signal were credited for the spike that already happened; under the accumulator the potential was reset at that spike, so the arrival (if not dropped as refractory) opens the NEXT interval. (b) The refusals are written against options the scope drops: LATE's other values (ignore, depress) belong to the perturb eligibility and the leaky eligibility is LEAKY_ELIGIBILITY/SYNAPSE_TAU, all dropped, so the specification has nothing to refuse.
  - *fix:* "The late-signal question does not arise. A signal that arrives while j is refractory is dropped and counts nothing; one that arrives after j's spike begins the next interval and is credited there. The trace says what each signal was contributing at each decision, so neither eligibility takes a late-signal rule or an eligibility trace of its own [record §6.7]." Keep the engine requirement (an engine asked for either refuses) only if LATE survives as a constant elsewhere in the specification.
- **out-of-scope** — 8.11, closing sentence ("equals it to about a part in 10^15")
  - A measurement smuggled into a clause. It comes from RECORD §6.7: "the regrouping was checked against the built per-decision sum on goo 40 at TAU = ∞: 88,601 comparisons, worst relative difference 2.5 × 10^{-15}". The number is a finding about a check that was run, not a rule an engine must meet, and the clause then has to talk its way out of it ("which is not bit equality").
  - *fix:* "This bookkeeping is the rule: it is a regrouping of the per-decision sum of 8.4 and is not required to reproduce that sum bit for bit. The engines are held to each other (8.15) [record §6.7]."
- **wrong-attribution** — 8.1 and 8.2, the attributions "(The pre-alpha's, factored out behind RULE = reinforce [record §6.7].)" and "(The pre-alpha's [record §6.7, §1.3].)"
  - No date and no person, where the file's own rule is "each clause numbered, dated and attributed". "Factored out behind RULE = reinforce" is also history of the code's shape, and RULE's other values (teacher, adaline, dopamine) all leave the specification, so the switch it names no longer exists. Neither clause says where its constants come from: LR = 0.03 and BASELINE_RATE = 0.05 are simply asserted (RECORD §6.7 has "LR 0.03 stands" after the Δ × LR grid, §1.3 carries both constants).
  - *fix:* Attribute both: the rule is Williams's [1], carried from the pre-alpha and asked for on this substance by Byron, September 13, 2026 ("I want to try the same thing with the REINFORCE algorithm"); cite §1.3 for LR and BASELINE_RATE as the standing constants. Drop "factored out behind RULE = reinforce".
- **wrong-rule** — 8.1, the clip ("clipped to WEIGHT_RANGE, the same [−1, 1] the weights were drawn from")
  - The code clips to the network's own range, not to a fixed [−1, 1]: `low, high = grid.weight_range` in learning.py, and RECORD §1.1 gives WEIGHT_EPSILON = 0.001, "under `--positive-weights` the range becomes [ε, 1]: no inhibition". learning.py's own summary says "weights are kept within the grid's weight_range, [-1, 1] by default".
  - *fix:* "clipped to the range the network's weights were drawn from — WEIGHT_RANGE, [−1, 1] by default and [WEIGHT_EPSILON, 1] under positive weights [record §1.1]".
- **wrong-attribution** — 8.9 The ISI factor weighs every charge
  - The clause decides a default and quotes nobody. RECORD §0.2 has Byron's words for both halves: "The desired ISI is 5.1 ms (hardcode for now)" and, choosing between on and off, "On by default". The closing sentence — "count_hebb, wrong_hebb and perturb had no per-decision charge to weigh; they are not in this specification" — is history about three dropped eligibilities and earns nothing here.
  - *fix:* Quote "On by default" (Byron, September 17, 2026) for the default and cite §0.2 for TARGET_ISI's "hardcode for now", which the clause should also carry as open (RECORD §1.3: TARGET_ISI "not known — hardcoded for now"). Delete the last sentence.
- **wrong-attribution** — 8.5, 8.11, 8.13, 8.15 — no date, no attribution
  - Four clauses carry neither a date nor a hand. 8.5 and 8.11 are Claude's derivation and Claude's bookkeeping on Byron's September 17 decision; 8.15 is the cross-platform rule, which is Byron's, September 14, 2026 ("rules in authority.md must be implemented cross-platform", RECORD §7) and September 17 ("keep, with tolerances, as a foundational rule; all platforms are built from one clear authority").
  - *fix:* Date and attribute each, as the record does, and say which parts are Claude's reading.
- **missing** — 8.2 / 8.10, what happens to the score when nothing is paid
  - Incomplete against the code. `reinforce()` settles the scores and then returns on `if not advantage: return 0`, and `Network.reset` clears every score at the start of the next epoch regardless — so on the first epoch (advantage exactly zero, 8.2) the epoch's whole eligibility is discarded, not carried forward to the next read. 8.10 says the read "pays the score charged since the last read and clears the score" without saying that the clearing happens whether or not anything moved.
  - *fix:* Add to 8.10: "The score is cleared at every read, whether or not the advantage moved a weight; what stays open across the read is the arrivals and their debit, not the score." Then 8.2's "nothing moves" is complete.
- **style** — 8.12 The leak path
  - Carries an argument ("the regrouping needs factors of e^{t/TAU} that overflow within a second of run") where the clause should carry a requirement. It is the record's reason (§6.7), not a rule.
  - *fix:* State the rule and the engine requirement: under the leak the charge is made per decision, the engine walking j's incoming synapses and adding f(c_j − q_j) times each trace decayed to the decision's time; the accumulator's bookkeeping of 8.11 is not used on this path [record §6.7].
- **style** — 8.3, title and body ("Two eligibilities, and the eligibility follows the neuron")
  - Two rules in one clause, against the file's "one rule to a clause": which eligibilities exist, and which one a run gets when it names none. The parenthetical rationale ("a decision that was not a draw has no probability to differentiate") is an argument from RECORD §6.7's derivation.
  - *fix:* Split: one clause naming the two eligibilities and the refusal (the hazard needs ESCAPE_DELTA > 0, learning.py raises), one clause for the default, which is the one that is part-open.
- **missing** — 8.14 What a checkpoint carries
  - The list is short of what persistence.py writes for this rule: `target_isi` is saved beside `isi_factor` (§0.2 pins it per network), and the teacher's `late` is saved beside `critic` and `eligibility`. The title also reads as though it described the whole checkpoint, where §7's invariant ("Checkpoints round-trip") covers the rest.
  - *fix:* Title it "What a checkpoint carries of this rule", add TARGET_ISI and the late-signal setting if that constant survives, and cite §7 for the round-trip invariant.
- **missing** — 8.3 / 8.4, hebb under escape noise
  - Unstated, and the code makes it a real case: in `Neuron.decide` the `if self.centred:` branch is taken before the hazard branch, so a run that names hebb on a network with escape noise still draws its firing decision but charges y − p̂ instead of the hazard's terms (the Rust loop says the same: "Keeps the traces whether or not there is escape noise"). A reader of 8.3 would take hebb to be available only where the threshold decides.
  - *fix:* One sentence in 8.4 or 8.3: the eligibility is chosen independently of what makes the firing decision; a run may name hebb on a network with escape noise, and then the decision is still a draw but is charged against the neuron's own expectation.
- **missing** — 8.1, the forced skip
  - Only half the rule. The record and learning.py also say the converse, which matters under the Poisson rate drive where an input neuron is forced only at its own arrivals: "An unforced input neuron is an ordinary neuron and is treated as one" (learning.py), the same first-class-citizens rule RECORD §6.7 cites for un-sticking ("all neurons are first-class citizens").
  - *fix:* Add: "An input neuron that was not driven this epoch is an ordinary neuron, and its incoming synapses are paid like any other's."

**Missing from this section:**
- The quash (RECORD §6.11, kept as a non-default) has no place in this part and no clause is promised elsewhere for it. It moves weights, it runs at a refire rather than at the read, and the record says it "runs under every rule, so it composes" — section 8 must either carry it or say where it lives, or the preamble's "one rule moves a weight" stands uncorrected.
- Homeostasis and un-sticking (kept as non-defaults): in the record they run at the same read as the update, in the same paragraph of §6.7, with the same forced-neuron exemption ("a neuron forced this epoch is left alone, as homeostasis leaves it") and with "nothing clips where they take it" (THRESHOLD_RANGE eliminated, Byron, September 14, 2026: "It's artificial"). Section 8 mentions neither; if they belong to the neuron's section, say so here in a line.
- The estimator's correlation (kept): Byron, September 16, 2026, "Please modify the code so we can plot the correlation of the estimator over time after a run" — the Pearson correlation and sign agreement of the weight change against the supervised direction d_ij, recorded at every trace interval (RECORD §8, `fast.train(direction=...)`, `mnist.supervised_direction`, the `estimator` field of each arm's record). It is an instrument on this rule and nothing in section 8 requires an engine to produce it.
- Nothing states, as a clause, that this is the only learning rule the specification carries. RULE (teacher, adaline, dopamine) leaves the specification with those rules, so the preamble's prose should become a numbered clause: one rule learns, it is paid once an epoch at the read.
- Note on the scope you were given: its critic list is stale against docs/rewrite-answers.md, whose last entry (15:12 MDT) is "keep the class critic" — four critics survive, and the fraction right is that critic's own number rather than a separate reported measure. It does not change any clause in section 8 (8.1 says only "the critic the run names"), but the critics clause must follow the answers file, not the scope list.
- On the relayed question, "drop the codings": docs/rewrite-answers.md settles it at 15:10 MDT — dropped are the coding schemes of RECORD §4.3 (raw, population, population-complement, the Hamming and parity codes, and the input flips FLIP); kept are complement coding on the inputs, complement coding on the outputs and the clock neurons, which is what mnist runs. Nothing in this draft states or assumes a dropped coding, so section 8 is clean on that point.

**Choices the drafter had to make:**
- "Drop the codings", confirmed against Byron's own confirmation of September 17, 15:10 MDT: dropped are raw coding, population coding, population-complement, the Hamming and parity error-correcting codes and the input flips (FLIP); kept are complement coding on the inputs, complement coding on the outputs and the clock neurons — what mnist runs. It touches nothing in section 8: the learning rule never sees a coding, only arrivals, decisions and one scalar reward, and no clause below names one.
- The fallback eligibility when the threshold decides is hebb (8.3). The record's default named the ELIGIBILITY constant, whose value was perturb; perturb is dropped, and hebb is the row derived for a spike that was caused rather than drawn, so it is the only reading that keeps the default working. Flagged in the clause as Claude's reading.
- The ISI factor is split: its form, its constant and its default belong to the clause at the head of the file (Byron asked for the factor's authority near the very top), and section 8 keeps only the mechanism — that it weighs both the credit and the expectation of every charge, at the decision, and that f = 0 before a neuron's first spike (8.9). This avoids both a gap and a duplicate clause.
- LATE is written as a clause (8.13) although only one value now runs, because the rule for a late signal is a real rule and a reader of the record will look for it; the clause says count and says the alternatives are refused.
- The read is written as one of the events in 8.11 rather than a clause of its own, so that the whole per-event bookkeeping of the accumulator reads as one rule; 8.1 carries the payment and 8.10 the fact that arrivals survive the read.
- Provenance is cited as [record §x] in square brackets, per the preamble's allowance, with dates and Byron's words inside the clause. No measurement, sweep, table of scores or derivational argument is carried over — the zero-in-expectation property, the +0.032 on wrong_hebb, the 88,601-comparison check of the regrouping and the history of wrong_hebb and count_hebb all stay in RECORD.md. The one exception is the part in 10^15 of 8.11, kept because it states what the engines are and are not held to.
- Section 8 is written as one section; nothing in my part needed a second.

**Left open for Byron:**
- Hebb's credit at the spike (8.8): full unit or discounted by the expectation. Byron, September 17: "Defer for now." The clause states the built behaviour and names it open.
- DECISION_MEMORY = 1e-4 is a starting value, to be swept (8.6); and the warm start by max(DECISION_MEMORY, 1/n) is Claude's reading of "changed per decision", to be corrected in a word.
- Which eligibility runs when the threshold decides (8.3). The record's default was "hazard under escape noise, else the ELIGIBILITY constant", and that constant was perturb, which the rewrite drops. I wrote hebb into the fallback; one word from Byron settles it.
- The ISI factor's own clause — the form f = (3x-1)/(1+x^3), TARGET_ISI 5.1 ms, on by default — is assumed to sit at the head of the file at Byron's instruction ("make the factor authority near the very top"). Section 8 states only how it enters the per-decision charge (8.9); the cross-reference needs the head clause's number on assembly.
- Section numbers for the clauses I cite by name — the firing clause (escape noise, m_j), the neuron's clause (the accumulator, TAU, the floor, the refractory period), the critic that produces R, and the invariants clause that names the array engine's tolerance — are cited as RECORD sections until the new file's numbering exists.
- Not in section 8, and needed elsewhere before it is complete: which critics produce R (evidence, row, graded), the fraction right as a reported measure, homeostasis, un-sticking and the rate memory as non-defaults, and the quash as a non-default. All of them are the Teacher's or the read's, not the update's.
- The score since the last read is not checkpointed (8.14). That is right only because a checkpoint is written between epochs, after the read has paid it; I wrote that as Claude's reading. If a checkpoint is ever taken mid-epoch, the field has to be added or the clause changed.
- Whether LATE and ELIGIBILITY survive as constants at all, now that LATE has one admissible value (count) and ELIGIBILITY two. Named here rather than decided.

## teacher (§9) — fixable

- **wrong-rule** — §9.7 (and §9.5's "one of the three critics")
  - The class critic is alive and the draft writes it out of existence. `docs/rewrite-answers.md` ends with a later answer than the three-critic one the draft rests on: **"The class critic, kept (15:12 MDT): *"keep the class critic"*. So four critics survive: evidence, row, graded and class -- the 1-or-0 that pays when the label's class out-spikes every other, a tie losing. The fraction right reported beside the evidence reward is that critic's own number, so the reported measure and the critic are one rule again rather than two."** The draft's §9.7 asserts the opposite in bold — "**No run is paid on it.**" — and §9.5 calls row "one of the three critics". A run asking `--critic class` is paid on exactly that number (RECORD §8, mnist decision 3: "The label's group must out-spike every other class's; the reward is then 1 and otherwise 0 — a tie loses, and so does silence"; `CRITIC` in RECORD §1.3 lists class; `learning.py` CRITICS carries it and `fast.train` pays it).
  - *fix:* Add a clause for the class critic between §9.5 and §9.6 — on the same class evidence, 1 when the label's class strictly out-spikes every other and 0 otherwise, a tie losing and silence losing [RECORD §8, §1.3] — attributed to Byron, September 17, 2026, 15:12 MDT, "keep the class critic". Change §9.5 to "one of the four critics" and §9.2's cross-reference to §9.4–9.7. Rewrite §9.7 as: the fraction right is the class critic's own number, reported beside whatever reward the run is paid; delete "No run is paid on it", and replace the "Claude's proposal, accepted in 'confirm and proceed'" attribution with the 15:12 answer, under which the measure and the critic are one rule.
- **wrong-rule** — §9.7, and the third bullet of §9.11
  - "Every run reports the fraction of epochs in which the label's class strictly out-spikes every other" and "wherever a reward is reported" state more than any engine does. In `src/walnutbutter/fast.py` the hits are counted only inside `if epoch >= epochs - tenth:` and only `if critic == "evidence"`, and the report carries `"accuracy_last_tenth": hits / tenth if critic == "evidence" else None`; the object engine's Teacher (`learning.py` step/report) never computes it at all. RECORD §8 says only "the Rust driver logs the class critic's fraction right beside it, so a fraction stays readable". Byron's settled wording is narrower too: "every run reports the fraction of epochs in which the label's class out-spikes every other, beside the evidence reward it is paid on".
  - *fix:* Either state the built rule — the class critic's fraction over the run's last tenth in a sweep arm's record, and every thousand epochs in the watch — or keep the wider requirement but mark it as new work the clause imposes ("Claude's reading, to be corrected in a word"), and say plainly what it requires of the engines: the object and array engines must grow the per-epoch report the Rust driver has.
- **style** — §9.7, second paragraph
  - "It is the number the project is read by, and keeping it a report rather than a critic is what lets the reward be the evidence critic's log score and the reading still be a fraction" is an argument for the rule, which AUTHORITY.md's own drafting rule excludes ("no sweep results, no findings, no arguments — those stay in the record").
  - *fix:* Cut the sentence; the bracketed [RECORD §8] provenance carries it. With the class critic restored the sentence is also no longer true as stated.
- **wrong-attribution** — §10.1, the opening quotation
  - The draft puts Byron's September 13 words inside quotation marks with an ellipsis. RECORD §6.11 does not present them as verbatim: "*Byron, September 13, 2026, turning the rule around:* when a neuron fires itself, it has found a cycle. Cycles may be BAD. The whole point of having an absolute refractory period is to quash short cycles. Sustain may be exactly what we don't want a neural network doing. We may have had the learning rule backward: cycles need to be quashed, so move the contributing weights in the direction that will actually do this." The draft also elides "Sustain may be exactly what we don't want a neural network doing. We may have had the learning rule backward:" behind an ellipsis, joining two sentences the record keeps apart. Two other quotations in the same clause are exact in the record and correctly marked ("the refire delay is evidence of how tight the loop is", "we will have to explore this space").
  - *fix:* Follow the record's form — italic attribution, reported speech, no quotation marks — or quote only the words the record itself marks as Byron's exact ones.
- **wrong-rule** — §9.9, §9.10 and §10.1 (the constants)
  - Each clause states the constant as 0 and then recounts what the record's runs did: "HOMEOSTASIS is **0** ... the rate the record's runs used when it was on is $10^{-6}$"; "UNSTICK is **0** unless a run asks; the rate the record's runs used when it was on is $10^{-3}$"; "QUASH_RATE is **0** ... the value the record's runs used when it was on is 0.02". RECORD §1.3 gives the constants as HOMEOSTASIS 10⁻⁶, UNSTICK 10⁻³, QUASH_RATE 0.02, each with "0 = off", and the off-by-default is a property of the library and the problem (`network.py`: `self.quash_rate = 0.0  # ... off until asked`; `problems.py` mnist `quash=False`; RECORD §8, "with homeostasis and un-sticking off", Byron: "Please turn off for this task"). Writing 0 as the constant's value and demoting the real value to what "the record's runs used" is both a change of constant made silently and a piece of history in a clause.
  - *fix:* State the constant at its value (HOMEOSTASIS 10⁻⁶, UNSTICK 10⁻³, QUASH_RATE 0.02, QUASH_K 0.2/ms), then the rule: it does not run unless a run asks for it, and 0 switches it off — Byron, September 17, 2026, "Homeostasis and un-sticking, as non-defaults" and "The quash (§6.11), as a non-default". Drop "the record's runs used" from all three.
- **out-of-scope** — §9.5
  - "One neuron right in twelve is $1/12$" sizes the example on a twelve-neuron output zone, which belongs to the dropped copy problems and to TEACHER_CREDIT (RECORD §1.3: "0.25 at four neurons, 1/12 at twelve"), both out of scope. The only problem the specification carries has 60 outputs (RECORD §8, decision 6: "**60 neurons, the ten fire-if-one populations of three first ... and then ... the ten fire-if-zero populations of three**").
  - *fix:* Drop the numeric example, or state it on mnist's zone; and say what the target pattern is under complement coding, as the record does: "The label code for the row critic is the label's one-group on, its zero-group off, and every other class the other way."
- **style** — §9.2 and §9.3
  - The section never says that the rule paying at the read passes over a forced neuron, though §9.2 carefully says the rate memory and the threshold moves do ("skipped by (3) and (4)"), which invites the reading that step (2) does not. The code skips it in every engine (`learning.py` reinforce: `if not connection.score or connection.target.forced: continue  # ... a forced input: its firing was not the network's doing`), and RECORD §6.7 makes it a §6 decision, not plumbing ("**The forced skip.** The update passes over every connection whose target was forced this epoch").
  - *fix:* One sentence in §9.2 saying the paying rule's own clause carries the forced skip, with the cross-reference, so the enumeration is not read as exhaustive.
- **style** — §9.4, the TEMPERATURE paragraph
  - "set at the middle of the first sweep's $\{1, 2, 4\}$" cites a sweep inside a clause. The value being open, and Byron's words, are what the clause needs; the sweep is the record's business.
  - *fix:* "TEMPERATURE is 2 **FOR NOW** — Byron, September 16, 2026: 'We will have to sweep for temperature eventually.' What is open is the value, which a sweep is to set. [RECORD §1.3, §8]"
- **style** — §10.2
  - The clause exists to name rules the specification drops ("leaky Hebb and the weight decay are dropped with the rules they were built beside"), which is the history the rewrite puts in RECORD.md; the task's own rule is that dropped rules "must not appear as clauses". The forward-looking half of the clause is worth keeping.
  - *fix:* Keep only the standing rule: the quash is the only local rule this specification carries, and a local rule added later states in its own clause where in the wave it acts relative to the quash and is off until a run asks for it [RECORD §6, the fixed within-wave order]. Let RECORD.md carry the list of what was dropped.

**Missing from this section:**
- The class critic itself — see the first problem. It is the only KEPT item of the scope that has no clause in this part.
- §9.3 says nothing about the baseline on resume, and the two resumes in the code disagree: `persistence.py` saves and restores it (`"baseline": teacher.baseline`, and on load `teacher.baseline = record["baseline"]`), while the sweep driver's `--resume-from` does not — RECORD §8: "not to the bit, since the exploration stream starts afresh (seed + 1,000,000) and the reward baseline from the first resumed epoch". Since resume is kept in scope, and §9.8 asserts that the rate memories and thresholds carry on, the baseline's behaviour on resume has to be settled in §9.3 (or named as open, in Byron's words, if it is).
- §9.11's end-of-run record names only each neuron's rate memory and threshold. The single-spike rule is kept, and its per-neuron state is in the arm's record for exactly the reason the code gives — `fast.py`: `"expectations": ...`, `"decisions": ...` with the comment "the single-spike rule's expectations and decisions to date (§6.7), so a continuation charges from where it was". Add p̂_j and the decisions to date (and the stuck counts, which the report already carries) to the list.
- §9.12 does not say which engines must provide the measurement. It is built in the Rust path only (`fast.train(direction=..., reference_weights=..., epoch_offset=...)`, `mnist.supervised_direction`); the object and array engines do not measure it. The clause's "what it requires of the engines" paragraph is missing, and it is the one place in this part where the one-authority rule needs a stated exception or a stated duty.
- Confirming the relayed request, "drop the codings": in `docs/rewrite-answers.md`, 15:10 MDT, September 17, 2026, it means the input and output *coding schemes* other than complement coding — "Dropped: raw coding, population coding, population-complement, the Hamming and parity error-correcting codes, and the input bit flips (FLIP)" — while "Kept: complement coding on the inputs, complement coding on the outputs, and the clock neurons -- what mnist runs." It is not a drop of coding as such, and it does not touch the class *evidence* read. Nothing in §9–§10 offends it: §9.4's n_k as the fire-if-one sum minus the fire-if-zero sum is the kept complement-coded read (RECORD §8, decision 6; `learning.class_evidence`), and §9.12's sign reversal on a fire-if-zero output is the same decision. The one place a dropped coding leaks in is the twelve-output example in §9.5, noted above.

**Choices the drafter had to make:**
- Put the composition principle at the head of §9 rather than §10, because what it decides first is that the teacher is the only rule paying at the read; §10 opens by pointing back at it, so it is stated once.
- Wrote the order at the read (score, pay, rate memories, homeostasis, un-sticking, baseline) as its own clause. The record states it only in the code and in §6.15's note that the book is mirrored; the engines have to agree on it, so it is a rule, marked as Claude's reading.
- Read "non-default" as off unless a run asks, so the three non-default rules default to 0 and the record's values are named as what a run asks for. mnist already asks for zero on all three (Byron, September 15, 2026: "Please turn off for this task"), so nothing he was running changes.
- Defined the quash's gate by the synapse's last-signal stamp landing after the previous spike -- exactly what all three engines test -- rather than the record's looser "carried a signal it integrated since that spike".
- Left the class evidence itself (the fire-if-one sum minus the fire-if-zero sum under complement coding) to the read's section and cited it from the critics, so the coding is specified once.
- Took "drop the codings" as keeping complement coding on inputs and outputs, so the class evidence stays a signed sum; the population critic's majority vote goes with population coding, and is not in these clauses.
- Kept the estimator's correlation as a reporting clause and said plainly that nothing reads the direction back, so it cannot be mistaken for a rule.
- Dropped the visualizer's colouring window and spike raster, and the dopamine and expectation columns, from §6.14's report list, since both go with dropped scope; kept "read as health, not convergence".
- Stated in §9.4 that the critics' arithmetic is bit-identical in all three engines (integer counts, one shared function with the max subtracted), so the one-authority tolerance is not invoked where it is not needed.

**Left open for Byron:**
- Defaults: I have written HOMEOSTASIS, UNSTICK and QUASH_RATE as 0 (off) and named the record's values (10^-6, 10^-3, 0.02) as what a run asks for, reading "non-default" that way. mnist is unaffected either way -- it sets all three to zero -- but the constants table has to agree, and Byron should say in a word whether the constant itself becomes 0 or stays at its value with every run switching it off.
- TEMPERATURE is 2 FOR NOW; the value is open and a sweep is to set it (Byron: "We will have to sweep for temperature eventually").
- QUASH_RATE and QUASH_K are open: "we will have to explore this space" (Byron, September 13, 2026).
- The row critic needs a target pattern, and with every problem but mnist dropped the only target left is `label`. Does TARGET survive as a constant (reversed, copy went with the dropped problems), and does the row critic keep a use on mnist?
- The class reading is kept only as the reported fraction right (§9.7), so `--critic class` is no longer one of the three payable critics. The code still offers it; that is a code change the clause implies.
- §9.11 says the fraction right is reported wherever a reward is reported. The Rust loop currently accumulates it over the run's last tenth only (mnist-watch reports it per window). If the clause stands as written, per-window reporting is a small gap to close.
- The baseline and BASELINE_RATE are in §9.3 because the teacher owns them; if the reinforce rule's section states them too, one of the two should go.
- The per-epoch trace's dopamine and expected columns go with the dropped rule, leaving epoch, time, reward. Confirm nothing else wants those columns.
- Cross-references to sections I do not own are by name (the reinforce rule, the read, the checkpoint, the escape noise's width) rather than by number; the numbers want filling when the file is assembled.
- To be corrected in a word: my reading of the order at the read (§9.2), and "the fraction right" as the name of the reported measure.

## problems (§10) — fixable

- **wrong-rule** — 10.15 The fraction right is reported, not paid — and the Appendix A CRITIC row
  - Both say the class critic is gone: 10.15 calls it "the class critic whose reading it is does not survive as a critic" and the register lists CRITIC as "row, graded or evidence". Byron reversed that two minutes after the answer the draft is built on. /home/byron/Documents/code/walnutbutter/docs/rewrite-answers.md, last entry: "**The class critic, kept (15:12 MDT):** *\"keep the class critic\"*. So four critics survive: evidence, row, graded and class -- the 1-or-0 that pays when the label's class out-spikes every other, a tie losing. The fraction right reported beside the evidence reward is that critic's own number, so the reported measure and the critic are one rule again rather than two." The task's scope line ("the critics evidence, row and graded") is the 15:02 list, not Byron's last word. The clause is also self-contradictory as drafted: it defines the reported fraction by exactly the class critic's rule (strict out-spiking, tie loses, silence loses) while denying that critic exists — learning.py:191-202 is one function, class_accuracy, and fast.py:492 computes the reported fraction by calling _reward(..., "class", ...).
  - *fix:* Restore class to the surviving critics in the Appendix CRITIC row (row, class, graded, evidence; mnist names evidence) and rewrite 10.15 as: the fraction right is the class critic's own number, reported beside the evidence reward and not paid on when mnist runs the evidence critic — quoting "keep the class critic" (Byron, September 17, 2026, 15:12 MDT) and dropping the sentence that the class critic does not survive.
- **wrong-rule** — 10.15 The fraction right is reported, not paid
  - "as a moving average over WINDOW epochs" states a rule the system does not have. WINDOW is the exponential moving average of the *reward*: learning.py:831-835 averages `reward` at alpha = 2/(WINDOW+1); RECORD §1.3 reads "WINDOW | 200 | epochs the reported moving-average accuracy spans (reporting only)" and §6.14 "the score of every epoch, its mean to date, and an exponential moving average over about WINDOW epochs". The fraction right is reported by the Rust driver over the last tenth of the run and only under the evidence critic: fast.py:489-492 (`if epoch >= epochs - tenth: ... if critic == "evidence": hits += _reward(..., "class", ...)`) and fast.py:509 `"accuracy_last_tenth": hits / tenth if critic == "evidence" else None`. "Every run reports" is likewise too strong.
  - *fix:* State what is reported: under the evidence critic the run's record carries the class critic's fraction right over the last tenth (`accuracy_last_tenth`) beside the reward, and the WINDOW moving average is of the critic's reward, not of the fraction. If a per-epoch or WINDOW-averaged fraction is wanted, that is a later change and must be named as an intention, not written as a clause.
- **wrong-attribution** — Appendix A, STUCK_BELOW, STUCK_ABOVE row ("reporting: a neuron whose observed rate is outside the band is read as stuck | 10.15")
  - The band is not a reporting constant: it gates the un-sticking rule. learning.py:541 `if neuron.rate > STUCK_ABOVE or neuron.rate < STUCK_BELOW: neuron.threshold = neuron.threshold + rate * (neuron.rate - target)`; arrays.py:761 and fast.py:189 the same. RECORD §1.3: "UNSTICK | 10⁻³ | per-epoch rate a stuck neuron's threshold moves toward UNSTICK_TARGET". The draft's own RATE_MEMORY row already says "which homeostasis, un-sticking and the stuck bands read", contradicting this row. It is also owned by 10.15, which never mentions it.
  - *fix:* Move the row to the un-sticking clause (the draft's §8, beside UNSTICK and UNSTICK_TARGET) and describe it as what makes a neuron stuck for the un-sticking rule, noted as also counted in the reports (stuck_on/stuck_off).
- **wrong-attribution** — 10.6 The output zone: 60 neurons, complement-coded
  - The evidence rule n_k = n_k⁺ − n_k⁻ is attributed wholly to Byron's quote, but the record marks it as Claude's: RECORD §8, decision 6 — "The read is unchanged, by count; *the scoring rule, Claude's reading of \"accordingly,\" to be corrected in a word:* the evidence for class $k$ is its fire-if-one sum minus its fire-if-zero sum". Byron's words settle the layout ("three fire-if-one and three fire-if-zero... We will need to change our scoring rule accordingly"); they do not settle what the new scoring rule is. AUTHORITY.md's own drafting rule requires the clause to say so.
  - *fix:* Split the attribution: Byron's quote for the zone (60 neurons, three and three, the ordering) and a second line — "Claude's reading of 'accordingly', to be corrected in a word" — for n_k = n_k⁺ − n_k⁻ [record §8, decision 6].
- **wrong-attribution** — 10.16 The estimator's correlation is reported
  - "The instrument is Claude's, built September 16, 2026 at Byron's question about where the learning was" is not in the record, and it understates Byron. The run-time instrument was built on his instruction, RECORD §8, September 16, 2026, 04:25 MDT: "Please modify the code so we can plot the correlation of the estimator over time after a run. Please build, commit, push, and SWEEP separately...". What is Claude's is the supervised direction itself, from the earlier gradient check (§8, `docs/mnist-evidence-gradient.py`; §0.1 states d_ij = P(pixel i on | class of j) − P(pixel i on)).
  - *fix:* Attribute the instrument to Byron's instruction of 04:25 MDT with that quote, and say that the supervised direction it measures against is Claude's, from the gradient check [record §0.1, §8].
- **missing** — Appendix A — the constant register ("Constants no clause yet claims")
  - PROBLEM is missing from the register entirely. constants.py:140 `PROBLEM = "reversal"` — the default problem, and reversal is dropped by the scope, so the constant is left pointing at a problem the specification does not carry. The register's own promise is "named here so they are decided rather than lost"; this one is lost.
  - *fix:* Add PROBLEM to the unclaimed list (or to 10.1): with mnist the only problem, PROBLEM is either mnist or goes, and the rebuild needs that decided.
- **missing** — 11.7 A seed is the whole run
  - "The wiring, the weights and the exploration draws all come from the seed's stream" reads as an exhaustive list and conflates two distinct Python streams while omitting a third kind of draw. (a) The drive's Poisson arrivals come from the network's own stream — RECORD §4.5: "The Poisson arrival times of §4.3 are *not* part of the stream: they depend on INTERVAL and on λ, which are themselves things a sweep varies, so they stay drawn per epoch from the network's stream" (network.py:483, `self._rng.expovariate(rate)`). (b) The escape-noise decision draws do NOT come from the build stream: learning.py:775 `self.rng = random.Random(seed)` and fast.py:420 `explore_rng = random.Random(seed) if (sigma > 0.0 or grid.hazard) else None  # the hazard's draws come from it too`. That second stream, not the build stream, is the one Rust takes over and hands back in 11.6.
  - *fix:* Name the streams a run has: the network's build-and-drive stream (wiring, weights, Poisson arrivals), the decision stream seeded from the same seed number (one uniform per neuron per wave, the stream Rust takes and returns, 11.6), and the input stream, separate (10.13). Then the invariant — one seed, the same run in any engine — is stated over a closed list.
- **missing** — 10.12 Homeostasis and un-sticking are off for this problem
  - mnist also switches the quash off, and no clause of this part says so: problems.py:169 builds the problem with `quash=False`. The scope keeps the quash as a non-default, so which way mnist sets it is a problem fact of exactly the kind 10.12 states for homeostasis and un-sticking, and 10.14's list of what mnist does not fix does not mention it either.
  - *fix:* Add the quash to 10.12 (mnist sets quash off, as the code has it) or give it its own short clause; if the record carries no Byron quote for it, mark it as built that way with the problem and to be confirmed in a word.
- **wrong-rule** — 11.4 What agreement means, and the one tolerance
  - "agrees **exactly on spikes**" and "That is the whole of the tolerance" claim more than the record supports. arrays.py:20-24: the object and array engines "differ only in the order floating-point additions happen, so on the rare wave where a potential sits within rounding of a threshold the two can decide differently and their runs diverge from there, like two seeds." RECORD §6.15 says the same and gives it as measured, not guaranteed ("two neuron-epochs in 800, at 5×10⁻¹⁷, on a goo of 40"), and under the hazard the decision is a continuous function of the potential. The number is also softer than the one the record's invariants and the tests enforce: §7 "same weights to 10⁻¹²" and tests/test_arrays.py:93-95 `np.allclose(..., atol=1e-12)` (1e-9 on potentials, line 141), against §6.15's "a part in 10⁹ on scores and weights".
  - *fix:* State the tolerance as the record does at both ends: continuous quantities to a part in 10⁹ as the rule, with the tests holding weights, thresholds and rates to 10⁻¹² absolute; and say plainly that spikes have agreed on every configuration compared but that a potential within an ulp of the decision can part the engines, which is why 11.8 requires the comparison on the configuration.
- **missing** — 10.1 mnist is the only problem
  - "Where mnist names a value the problem's value stands; where it names none, the constants of Appendix A do" leaves out the run, which overrides both. cli.py:814-819: `if problem.lr is not None and args.lr == LR: args.lr = problem.lr` — and the same for homeostasis and unstick — so the order is the command line first, then the problem, then the constant. 10.11 and 10.12 say "unless the run says otherwise", so 10.1 is out of step with its own section.
  - *fix:* State the precedence once, in 10.1: the run's own value, else the problem's, else the constant.
- **missing** — 10.13 The stream
  - The clause gives the shuffle's formula but never says where the seed number in it comes from, which is the only thing that makes "seed s shows the same images in the same order to any network" checkable. cli.py:1053: `dataset_stream(args.data, args.input_seed if args.input_seed is not None else seed)` — it is `--input-seed` when given, otherwise the run's seed.
  - *fix:* Name it: the shuffle's seed is `--input-seed` where a run gives one and the run's own seed otherwise, and it indexes only the shuffle, never the network's build.
- **style** — 11.5 The summation order is part of the rule; 11.10 A rule is recomputed, a state is stored
  - Both close with an argument for the clause rather than provenance: "Without this clause 11.4's `==` cannot be met..." and "This is what makes 11.9's round-trip decidable when a new field appears." AUTHORITY.md's drafting rule is "no sweep results, no findings, no arguments — those stay in the record".
  - *fix:* Cut both sentences to the bracketed record citation (§6.15; §5.2, §7). The consequence for 11.4 can be carried by 11.4 naming push order as what its `==` rests on.
- **style** — 11.8 Agreement is proven on the configuration, not carried over
  - The clause retells two record findings inside itself — "a wave stamp that only bit a neuron left untouched for a whole epoch, and a neuron with no incoming synapses that one engine silenced where the others fired it". Both are accurate (RECORD §6.15 the wave-stamp bug; §5.2 "the array engine divided by the zero width, took NaN for the chance and silenced it instead"), but they are history of what went wrong, which the file sends to the record.
  - *fix:* Keep "Claude's reading, to be corrected in a word" and the bracketed citations [record §5.2, §6.15, §7]; drop the retelling of the two cases.
- **style** — Appendix A, ELIGIBILITY row
  - The value is given as "hazard" where the record's constant is perturb — RECORD §1.3 "ELIGIBILITY | perturb" and constants.py:188 `ELIGIBILITY = "perturb"`. The draft's value is right for the system as run (learning.py:743 `eligibility = "hazard" if getattr(grid, "hazard", False) else ELIGIBILITY`, and escape noise always fires the neuron under §5.2), and the "Claude's reading" tag is the record's own, but a reader of both files is left thinking the record says hazard.
  - *fix:* Say in the row that the record's stored default was perturb, which the scope drops, so the constant's value becomes hazard — the one the neuron already chose under escape noise [record §1.3].
- **style** — 10.2 The data
  - "in `mnist/`, which is not in git" is not quite the arrangement: .gitignore lines 10-11 ignore `mnist/*.gz` and `mnist/*-ubyte`, and `mnist/README.md` — which holds the sizes and sums the clause points at — is tracked.
  - *fix:* "the data files are not in git (`.gitignore`); `mnist/README.md`, which records the source, sizes and sums, is."
- **style** — Appendix A, ownership column (TEMPERATURE §10.9, POPULATION §5, TAU/REFRACTORY §1 against REFRACTORY_HOPS/INTERVAL §3)
  - The ownership column crosses its own rule that a value has one home. TEMPERATURE is owned by 10.9, the mnist clause, though the evidence critic survives for the whole specification and CRITIC is owned by §8. POPULATION is owned by §5 while what fixes it is 10.6. TAU and REFRACTORY sit in §1 while REFRACTORY_HOPS and INTERVAL, the record's neighbours in §1.2, sit in §3.
  - *fix:* Own TEMPERATURE with the evidence critic (§8) and let 10.9 cite it; own POPULATION where the complement-coded zone is stated; keep the neuron's clock constants together. The note that section numbers are provisional covers the digits, not which clause owns a value.

**Missing from this section:**
- The class critic, as a critic. Byron kept it after the answer this draft was built on (rewrite-answers.md, 15:12 MDT: "keep the class critic"), so §10/Appendix must carry four critics — evidence, row, graded, class — and 10.15 becomes the class critic's number reported beside the evidence reward rather than a measure with no critic behind it.
- mnist's quash setting. problems.py:169 builds the problem with quash=False; the quash is kept in scope as a non-default, so which way the problem sets it belongs beside 10.12's homeostasis and un-sticking.
- PROBLEM, the default-problem constant (constants.py:140, currently "reversal"). The register names every other unclaimed constant "so they are decided rather than lost" and misses this one.
- Where the mnist shuffle's seed number comes from (--input-seed, else the run's seed; cli.py:1053) — 10.13 gives the formula without the input to it.
- The drive's Poisson arrival times in 11.7's account of what a seed fixes (record §4.5: they "stay drawn per epoch from the network's stream", network.py:483), and the separation of the network's build stream from the decision-draw stream that 11.6 hands to Rust (learning.py:775, fast.py:420).
- The rest of what a run reports. The register's WINDOW row points at 10.15, but the WINDOW moving average is of the critic's reward (learning.py:834; record §6.14), and the reward's mean to date, the stuck-neuron counts and the un-stuck count (fast.py:508-509) have no clause in this part. Either 10.15 becomes a reporting clause that states them, or the register's WINDOW and STUCK rows need a home that exists.
- Two small items of checkpoint state in 11.9's enumeration, both stored and both moved by the run: each neuron's firing-rate memory (persistence.py:85 "rates") and its potential (line 93). The clause's list is read as what must round-trip.

**Choices the drafter had to make:**
- "Drop the codings" (Byron, September 17, 15:10 MDT) is read as: raw coding, population coding, population-complement coding, the Hamming and parity error-correcting codes and the input bit flips (FLIP) leave the specification. What stays is complement coding on the inputs, complement coding on the outputs and the clock neurons -- what mnist runs. So sections 10.5 and 10.6 carry complement coding twice and no other coding appears anywhere in my part.
- A consequence of that reading, stated so it is not mistaken for a survival: POPULATION 3 stays in the register, but only as the neurons per class in each half of the complement-coded OUTPUT zone (10.6). Population coding of inputs is gone; the word 'population' survives as a group of output neurons, not as a code.
- The four IDX files: the brief says four, and MNIST is distributed as four, but only the training split's two are held and fetched (Byron: "we do not dare touch the test split"). 10.2 says distributed as four, held as two, and 10.3 carries the refusal as its own clause.
- The mnist goo's size: the record's "644 in all" is the 50-output layout. With the complement-coded zone of 60 (10.6) the default goo is 654, and 455 with no hidden neurons. I stated the sum rule and both numbers rather than repeat 644.
- TAU is entered in the register at infinity (the accumulator) with 2 ms as the kept option, reversing the record's row, per Byron's "NOT leaky, but please leave the leak option with its analysis".
- The count read (10.8): mnist's evidence critic reads the counts themselves and never consults TEACHER_THRESHOLD, which only scores a read as bits. I kept TEACHER_THRESHOLD in the register because the row critic survives, and said in the clause where it does and does not apply.
- The fraction right (10.15) is stated with the class critic's exact definition -- strictly out-spikes, a tie loses, silence loses -- without naming the class critic as a critic, since it is dropped as one.
- Section 11 splits what the outline's 11.7 had as one invariant: 11.9 (a checkpoint round-trips, which is a bit-exact promise) and 11.11 (a resume, which is a continuation and is not). 11.5 (push order is normative) and 11.10 (a rule is recomputed, a state is stored) are added because 11.4's '==' and 11.9's round-trip are not decidable without them.
- No numbers from any sweep appear in either section: the estimator's correlation and the fraction right are defined as instruments (10.15, 10.16) and their values stay in the record.

**Left open for Byron:**
- TEMPERATURE's value is open in Byron's own words ("We will have to sweep for temperature eventually"); 2 is held until the sweep sets it.
- 10.14: what mnist does not fix is Claude's reading. The record's mnist runs gave the epoch length (100 ms), the starting threshold (0.6) and the floor (-2.4) on the command line; whether those belong to the problem or to the constants is Byron's to say.
- 11.11: what a resume must reproduce is not decided. The clause states the record's continuation as it stands (exploration stream afresh at seed + 1,000,000, baseline from the first resumed epoch, input stream advanced rather than restored).
- 10.15: the fraction right as a reported measure rests on "confirm and proceed" and is Claude's proposal; it wants a word from Byron, as does whether the moving average stays at WINDOW 200.
- The input zone under ff2 and ff2-partial is 395 neurons that hear nothing. The firing section's rule for a neuron with no incoming synapses (the container's quoted threshold and floor, scale 1, firing at the hazard's rest) is what makes mnist's input zone well defined; the collator must keep that clause and 10.5 pointing at each other.
- ELIGIBILITY's default (hazard) is Claude's reading of the record's "follow the neuron" rule now that escape noise always decides; hebb is the other surviving value and Byron has not named a default.
- Five constants no clause claims: ACROSS, WEIGHT_EPSILON, RULE, TARGET, LATE (listed at the foot of Appendix A).
- The register's section numbers are the outline's scheme and must be made exact once every drafter's clause numbers exist; a few rows (THRESHOLD/MINIMUM_POTENTIAL as the unit goo's pair is quoted in) may move between the neuron and firing sections.

